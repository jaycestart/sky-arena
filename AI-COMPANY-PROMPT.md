# AI 회사 만들기 — 프롬프트

아래를 그대로 복사해서 새 세션에 붙여넣으면 된다.
한 번 실패하고 전부 철거한 뒤 다시 만드는 것이라, **그때 왜 실패했는지**를
프롬프트 안에 못 박아 두었다. 그게 이 문서의 핵심이다.

---

```
SKY ARENA 프로젝트에 자율 AI 회사를 만들어줘.

## 지금 있는 것

- 게임: https://sky-arena-kg67.onrender.com  (Render 자동 배포, main 에 푸시하면 반영)
- 코드: https://github.com/jaycestart/sky-arena  (공개, 계정 jaycestart)
- 이미 작동 중: .github/workflows/ci.yml (커밋마다 문법·정합성·봇 60초 전투 검사)
- 남은 숙제: BACKLOG.md 참고
- 프로젝트 규칙과 실패 기록: RESUME.md 참고 (특히 '막다른 길' 항목)

## 원하는 것

사람이 지시하지 않아도 스스로 게임을 개선하는 조직.
매일 일하고, 결과를 사람이 검토할 수 있는 형태로 제출하고,
하루를 정리해 보고한다.

## 반드시 지킬 순서 — 이걸 어겨서 지난번에 실패했다

1. **직원 한 명만 먼저 만든다.**
2. **그 한 명이 실제로 결과물을 내는지 네 눈으로 확인한다.**
   확인이란 "실행됐다"가 아니라 "산출물이 실제로 생겼다"이다.
   지난번엔 26명을 한꺼번에 배치했고, 실행 기록은 남는데 PR·이슈·파일이
   전부 0건이었다. 그런데도 그걸 확인하지 않아서 하루를 버렸다.
3. 확인된 뒤에만 늘린다.

## 반드시 먼저 확보할 것 — 결과를 볼 수 있는 경로

지난번 실패의 진짜 원인은 **직원 안에서 무슨 일이 나는지 볼 수 없었다**는 것이다.
실행 로그가 빈 화면이라 원인을 네 번 추측했고 네 번 다 틀렸다.

그러니 어떤 방식으로 만들든, **네가 직접 읽을 수 있는 로그**가 있어야 한다.
읽을 수 없는 인프라 위에는 짓지 마라.

참고로 GitHub Actions 는 이 조건을 만족한다 — 로그가 API 로 읽히고,
저장소 쓰기 권한이 기본 제공되고, 이미 여기서 검사원 3명이 잘 돌고 있다.
반면 claude.ai 예약 루틴(routines)은 지난번에 산출물을 0건 냈고 원인 불명이다.
그걸 다시 쓰려면 **한 명으로 먼저 증명하고** 나서 써라.

## 안전 규칙

- main 에 직접 푸시 금지. 브랜치 + PR 로만. main 은 실서비스로 자동 배포된다.
- 사람이 Merge 를 눌러야 반영된다. 자동 병합 금지.
- 한 번에 일감 하나만. 여러 개를 섞으면 무엇이 무엇을 망가뜨렸는지 못 되짚는다.
- 원인을 못 찾았으면 "못 찾았다"고 쓰게 하라. 추측으로 고치고 '고쳤다'고
  하지 못하게. 이 프로젝트는 그럴듯한 오진으로 시간을 가장 많이 버렸다.
- 조작·무기·물리를 바꾸면 봇에도 같이 적용 (_bot_point, _bot_think).
- 서버 game.py 물리와 클라 world.js 예측은 같은 공식이어야 한다.
  한쪽만 고치면 화면이 떤다.

## 이 프로젝트의 특수 조건

- 의존성 0. 파이썬 표준 라이브러리 서버 + 손으로 쓴 WebGL.
  빌드 단계도 패키지 매니저도 없다. requirements.txt / package.json 만들지 마라.
- **화면을 눈으로 볼 수 없다.** 렌더 결과 확인이 불가능하므로
  "좋아 보인다"는 근거가 아니다. 숫자로 증명해야 한다.
  `cd server && python -c "..."` 로 게임 로직을 직접 돌려 측정할 수 있다.

## 완료 기준

- 직원이 최소 한 번, 실제로 산출물을 냈다 (PR 또는 이슈가 눈에 보인다)
- 그 산출물을 내가 열어서 확인했다
- 매일 자동으로 도는 일정이 걸려 있다
- 사람이 결과를 보는 방법이 정해져 있다 (어디를 열면 되는지)

먼저 계획을 말해주고, 내가 동의하면 한 명부터 만들어줘.
```

---

## 왜 이렇게 썼나

지난번 실패에서 나온 것만 담았다.

| 지난번 | 프롬프트에 넣은 대응 |
|---|---|
| 26명을 한꺼번에 배치 | "한 명만 먼저, 확인 후 확장" |
| 실행됐다고 착각 | "산출물이 생겼는지가 확인이다" |
| 로그를 볼 수 없어 4번 오진 | "읽을 수 있는 인프라 위에만 지어라" |
| 어느 계정으로 도는지 몰랐다 | 저장소·계정을 명시 |
| 자동 병합 위험 | "사람이 Merge 를 눌러야" |
