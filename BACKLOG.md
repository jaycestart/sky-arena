# 남은 일감

2026-08-13 정리. AI 회사(자율 에이전트)는 해체했다 — 사람이 안 본 코드가
배포돼 게임이 망가진 적이 있다. 여기 적힌 것은 사람이 판단해 골라 간다.

## 규칙

1. **한 번에 하나만.** 여러 개를 같이 건드리면 무엇이 화면을 망가뜨렸는지
   되짚을 수 없다.
2. **고쳤다고 주장하려면 숫자를 대라.** 이 저장소의 검증은 전부 헤드리스로
   돌릴 수 있다(`.github/workflows/ci.yml`). 화면을 눈으로 못 보는 환경이라
   "좋아 보인다"는 근거가 아니다.
3. **`RESUME.md` 의 '막다른 길'을 먼저 읽어라.** 이미 여러 번 실패한 접근이
   적혀 있다. 다시 시도하면 시간만 버린다.
4. 조작·무기·물리를 바꾸면 **봇에도 같이 적용**한다(`_bot_point`, `_bot_think`).
5. 서버가 내려보내는 형식을 바꾸면 **클라이언트가 읽는 곳도 같이** 고친다.
   한쪽만 고쳐서 실제로 게임이 망가진 적이 있다(2026-08-13). CI 의 "서버가
   보내는 칸 수와 클라가 읽는 칸 수가 맞는가"가 그때 생긴 검사다.
6. 일감을 끝내면 여기서 지우고, 새로 발견한 것은 아래에 추가한다.

---

## 새로 나온 것 — 상승한계가 고도를 안 막고 속도를 지운다 (2026-08-15, QA실)

**번호를 안 붙였다.** 아래 1~8순위는 다른 실 파일의 주석이 번호로 인용하고
있어(`hud.js` "BACKLOG 4순위", `game.py` "BACKLOG 8순위") 한 칸씩 밀면 그
주석이 전부 어긋난다. 순위는 사람이 정할 것.

`_check_bounds` 가 천장 위에서 하는 일은 한 줄이다(game.py:581).

```python
if p.pos[1] > CFG["ceiling"]:      # 17,000m
    p.vel[1] = min(p.vel[1], 0.0)
```

**이 줄은 고도를 못 막는다.** `World.step` 은 `p.step()`(여기서 위치를 이미
적분했다) → `_check_bounds` 순서고, 다음 틱 `Plane.step` 은 `vel = fwd(q)·spd`
로 속도를 **통째로 다시 만든다**. 깎아 둔 `vel[1]` 이 위치에 쓰이는 경로가
없다. 한 틱을 뜯어 보면 이렇다.

| | 고도 | vy |
|---|---|---|
| 틱 전 | 17,500.0 | +900.0 |
| `p.step()` 뒤 | 17,518.3 | +913.9 ← 위치는 여기서 적분된다 |
| `_check_bounds` 뒤 | 17,518.3 | **+0.0** ← 여기서 깎는다 |
| 다음 `p.step()` | 17,518.9 | +29.9 ← vel 을 다시 만든다 |

무적으로 두고(봇 없이) 89도 전속 상승 120초 — **18,590m 까지 올라가고 여전히
상승 중**이다. 천장은 없다.

**대신 속도를 지운다.** 깎인 `vel` 이 다음 틱의 `cur = |vel|` 로 들어가므로
속도 **크기**가 y 성분만큼 잘린다. 정상상태는 `spd = 14/(1 − cos θ)` (θ=기수각,
arcAccel 700 · dt 0.02 에서 틱당 회복 14m/s), 상한은 `arcBoost` 1450 이다.

| 기수각 | 천장 위 SPD | HUD 마하 |
|---|---|---|
| 5도 | 1,444 | 4.25 |
| 20도 | 218 | 0.64 |
| 45도 | 33.8 | 0.10 |
| 60도 | 14.0 | 0.08 |
| 89도 | 0.2 | 0.00 |

`1450 = 14/(1−cos θ)` 를 풀면 **7.97도**다. 그보다 가파르게 오르는 순간부터
속도가 깎이고, 60도에서는 1,450 → 14.0 이 **한 틱 만에** 일어난다.

**닿기 쉽다.** 출격 고도 3,340m 에서 전속으로 기수를 세우면 89도에서 9.9초,
60도에서 12초면 천장을 넘는다. 넘는 순간 마하 4.26 이 0.08 로 떨어지고,
기수를 8도 아래로 내릴 때까지 그대로다. 되돌아오는 길은 있지만 **화면에
이유가 하나도 안 뜬다** — 클라이언트 전체에서 `ceiling` 을 언급하는 곳이
0건이고 `world.js` 에 상승한계 분기가 없다.

**되튐은 문제가 아니다.** `world.js` 가 스냅샷마다(`_applyMe`) 서버 상태로
되감으므로 40ms 사이에 벌어지는 폭은 평균 0.47m · 최대 28.2m 다. 처음에
클라 모델을 자유 주행시켜 3초에 4km 가 벌어지는 것을 재고 '되튐이 심하다'고
적을 뻔했는데, 되감기를 넣으니 작았다. **문제는 서버가 권위로 속도를 지운다는
것 자체다.**

**봇은 안 걸린다.** 봇 6기 60초에서 최고 고도 8,216m, 천장 위 체류 시간
0.00% 다. 그래서 **CI 의 봇 전투 검증은 이걸 구조적으로 못 잡는다.** 사람만
걸리는 자리다.

수평 이탈(`worldR` 4,500m)은 초당 20 피해로 벌하고 HUD 가 알려 준다. 수직은
벌도 없고 안내도 없고 속도만 조용히 죽는다 — 같은 '전장 밖'인데 규칙이 다르다.

**고르는 길은 셋이다.**
1. **천장을 진짜로 만든다** — 위치를 자르고 기수를 눌러야 한다. 물리 변경이라
   `world.js` 예측과 봇까지 **같은 커밋**이어야 한다(규칙 4·5).
2. **수평 이탈과 같은 규칙으로 벌한다** — 천장 위에서 HP 를 깎고 HUD 에 이유를
   띄운다. 서버 + HUD 로 끝나고 `world.js` 예측은 안 건드린다.
3. **걷어낸다** — 클램프를 지우면 서버가 클라와 완전히 같아져 예측 오차가
   0 이 된다. 천장은 어차피 안 막고 있었으니 잃는 것은 없다. 대신 사람이
   끝없이 올라갈 수 있다.

**QA 는 2번을 권한다** — 수평과 규칙이 같아지고, 화면에 이유가 뜨고,
`world.js` 를 안 건드려 예측이 어긋날 여지가 없다. 다만 어느 쪽이든 게임 규칙을
정하는 일이라 지시 없이 손대지 않았다.

관련 파일: `server/game.py` (`_check_bounds`, `CFG["ceiling"]`)

### 곁다리 — 아무도 안 읽는 스냅샷 칸이 이제 하나 남았다

`me.de`(사망 수) · `me.w`(선택 무기)를 읽는 곳이 **클라이언트 전체에 0건**이다.
2026-08-14 에 걷어낸 `aoa`·`bo`·`am`·`ms` 와 같은 부류다.

| 칸 | 왜 죽었나 | 지금 뺄 수 있나 |
|---|---|---|
| `de` | 리더보드 행이 사망 수를 따로 싣는다(`leaderboard`) | ~~뺄 수 있다~~ **2026-08-15 뺐다** |
| `w` | `_launch` 가 `p.weapon` 을 아예 안 본다 | 8순위와 묶여 있다 |

`w` 는 8순위(1·2 키)를 (a) 로 정하면 같이 빠지고, (b) 로 정하면 살아난다.
**그 결정 전에는 손대면 안 된다.** `de` 만 뺀 실측은 me 361 B → 354 B
(−7 B, 사람당 −175 B/s, 24인 만원 방 −4.1 KB/s)다. 아래 '해결된 것' 참조.

## 새로 나온 것 — 난전이 커지면 틱이 예산을 7배 넘는다 (2026-08-15, 인프라실)

**번호를 안 붙였다** — 위 QA실 항목과 같은 이유다(코드 주석이 순위를 인용한다).

정기 점검 '서버 부담'에서 걸렸다. 나머지 셋(배포·빈 방·깨어 있음)은 정상이다.
일곱 부서가 동시에 도는 기계라 벽시계 평균이 3배씩 흔들려서(같은 시험에서
9.5 · 17.2 · 28.6ms), **다섯 번 돌린 틱 중앙값 p50** 으로 적는다. 남의 부하는
봉우리를 만들지 중앙값을 밀지 않는다.

| 조건 | 틱 p50 | 기준 | 스냅샷 최대 | 기준 |
|---|---|---|---|---|
| 봇 6대 평상시 | 1.16ms | 15ms | 2.4KB | 10KB |
| 24대 자연 교전 | 16.12ms | 18ms | 9.0KB | 10KB |
| 24대 강제 연사 | **127.55ms** | 18ms | **23.5KB** | 10KB |

'강제 연사'는 봇 24대가 양손 방아쇠를 계속 누른 상태다. 사람이 그렇게 치지는
않지만, 24인 난투에서 다 같이 쏘는 순간과 같은 조건이다. 자연 교전은 통과하되
**여유가 없다**(16.12 / 18ms). 실서버는 지금 봇 6대라 `lag 0` 이다.

### (1) 비용이 미사일 수에 초선형이다 — 범인은 `미사일 × 플레어` 다

`_step_missiles` 는 표적을 가진 유도탄마다 **매 틱 `self.flares` 전체를 훑는다**
(`game.py:704`). 미사일과 플레어는 난전에서 같이 늘어나므로 곱으로 커진다.
플레어 투하만 끄고 같은 미사일 구간을 다시 재서 기여분을 분리했다.

| 동시 미사일 | 플레어 끔 | 현재 코드 | 그때 플레어 수 |
|---|---|---|---|
| 200~ | 20.20ms | 15.17ms | 0개 (아직 안 떨어졌다) |
| 300~ | 33.80ms | **103.03ms** (3.0배) | 186개 |
| 400~ | 26.65ms | **117.03ms** (4.4배) | 320개 |

플레어를 끄면 200~400발 구간이 20~34ms 로 **평평하다**. 즉 미사일만으로는
선형(미사일 × 기체)이고, 초선형 항은 704줄 하나에서 나온다. 안쪽에서
`fl.owner != tgt.id` 로 거르므로 벡터 연산은 안 하지만, 500 × 350 = 17만 번의
파이썬 루프가 매 틱 돈다. **표적 소유자별로 플레어를 묶어 두면 곱이 사라진다.**

### (2) 스냅샷의 58%가 플레어다 — 플레어에는 상한이 없다

23.5KB 짜리 스냅샷을 뜯으면 이렇다(캐시 비운 최악, 24대 강제 연사).

| 칸 | 개수 | 크기 | 몫 |
|---|---|---|---|
| `fl` 플레어 | 352 | **13.11KB** | **58%** |
| `m` 미사일 | 186 (상한에 걸림) | 7.43KB | 33% |
| `p` 기체 | 24 | 1.44KB | 6% |
| `me` | 1 | 0.34KB | 1% |

미사일 쪽 상한은 제대로 일하고 있다(564발 중 186발만 실린다). 그런데
`game.py:1111` 의 플레어는 **시야 안이면 전부 싣는다.** 1094줄 주석의
"미사일은 수가 폭발적으로 늘어나는 유일한 항목이다"가 이제 사실이 아니다 —
플레어가 미사일보다 크다. 미사일과 같은 방식(가까운 것부터 상한만큼)이면 된다.

### (3) `maxMissiles` 는 틱 비용의 손잡이가 아니다

인프라실 지시문은 부담이 넘치면 `CFG["maxMissiles"]` 를 돌리라고 하는데,
이 값은 `game.py` 에 **두 곳**밖에 없다 — 68줄(정의)과 1099줄(**스냅샷**)이다.
`_launch` 는 상한 없이 `self.missiles.append` 한다. 즉 이 값은 **대역폭
손잡이일 뿐 월드의 미사일 수를 못 막는다.** 틱 비용에는 영향이 0이다.

스냅샷 크기에도 부족하다. 40 까지 낮춰도 미사일 몫 7.43 → 약 1.6KB 라
합계가 약 17KB 로 여전히 10KB 위다(플레어 13.11KB 가 남는다). 게다가 자연
교전의 동시 미사일은 73~108발이라, 40 으로 낮추면 **평범한 교전에서 미사일이
화면에서 사라지기 시작한다.** 그래서 **손잡이를 돌리지 않았다.**

측정 스크립트는 저장소 밖(`_skyarena_probe/infra/probe2~5.py`)에 뒀다.
다시 재려면 그것을 돌려라 — 봇 24대를 만들고 `_fill_with_bots` 를 끈 뒤
`_bot_think` 를 감싸 방아쇠를 붙잡는 식이다.

### (4) 정정 — 범인은 704줄이 아니라 `_sweep` 이다 (같은 날 다음 인프라실 근무)

위 (1)~(3) 을 독립으로 다시 쟀다. **(2) 스냅샷과 (3) 손잡이는 그대로 맞다**
— 아래에서 따로 확인한다. **(1) 의 범인 지목과 틱 수치만 고쳐 적는다.**
고치기 전에 이걸 읽어라. 704줄을 손보면 14.93ms 중 0.69ms 가 돌아온다.

**먼저: 이 기계의 벽시계는 증거가 안 된다.** 같은 시험(24대 강제 연사,
시드 7, 6초)을 연달아 세 번 돌렸다.

| | 704줄 반복 횟수 | 틱 p50 |
|---|---|---|
| 1회 | 9,147 | 14.89ms |
| 2회 | 9,147 | 11.79ms |
| 3회 | 9,147 | 17.94ms |

**일의 양은 비트 단위로 같은데 벽시계만 1.5배 흔들린다.** 봇 6대 시험도
02:05 에 p50 0.32ms·최대 1.83ms, 02:15 에 p50 2.76ms·최대 **879.93ms** 였다.
강제 연사에서는 한 틱이 **1,163초** 걸린 적도 있다 — 프로세스가 통째로
밀린 것이다. 그래서 위 표의 `127.55ms` 도, 이번에 내가 잰 `29.66ms` 도
코드의 성질이 아니다. **p50 을 여러 번 돌려도 마찬가지다**(중앙값은 경합에
안 밀린다는 가정이 일곱 부서가 동시에 도는 이 기계에서는 성립하지 않는다).
고친 뒤 '빨라졌다'를 증명할 때는 **반복 횟수**를 세라. 그건 시드만 같으면
재현된다.

**범인.** 두 루프를 실제 객체로 따로 재고 틱당 횟수를 곱했다(강제 연사
동시 미사일 533발·플레어 348개·기체 24대 상태).

| | 한 번의 값 | 틱당 횟수 | 틱당 비용 | 몫 |
|---|---|---|---|---|
| 704줄 몸통 | **16.90 us** (표적 하나가 플레어 348개 훑기) | 41발 | **0.69ms** | 4.6% |
| `_sweep`(778줄) | **18.48 us** (기체당 769ns) | 514발 | **9.50ms** | **63.6%** |

`_step_missiles` 전체가 14.93ms(5회 중 최소)다. **`_sweep` 이 704줄의 13.7배**다.
cProfile 200틱도 같은 쪽을 가리킨다 — `_sweep` 누적 14.484s /
`_step_missiles` 누적 18.965s = 76%. (cProfile 은 **함수 호출마다** 값을
붙여서 `_sweep` 을 과대·704줄을 과소 평가한다. 그래서 미세측정을 따로 했고,
두 방법이 같은 쪽을 가리켰다.)

*첫 측정에서 704줄을 0.35ms 로 적었다가 0.69ms 로 고쳤다.* 안쪽을
`fl.owner != tgt.id` 비교(24.4ns)로만 쟀는데, **표적 소유의** 플레어는 그
아래 `v_sub`·`v_len`·`v_norm`·`v_dot`·`random` 분기로 들어간다. 플레어
348개가 24대에 흩어져 있어 표적 하나당 평균 15.6개가 그 비싼 가지를 탄다.
위 표는 실제 몸통을 그대로 베껴 다시 잰 값이다. 결론은 안 바뀐다.

**왜 704줄이 싼가 — 수를 세면 보인다.** 동시 미사일 453발의 구성:

| | 수 | 매 틱 하는 일 |
|---|---|---|
| 무유도 로켓 | 298 | `seeker<=0` 이라 유도를 통째로 건너뛴다 |
| 유도탄, 표적 보유 | **31** | 플레어 전부 훑는다(704줄) |
| 유도탄, 표적 탐색 | 0 | — |
| 무장 완료 | **432** | **기체 전부 훑는다(`_sweep`)** |

**미사일의 3분의 2가 무유도 로켓이라 704줄에 아예 안 들어온다.** 반복 횟수는
둘이 비슷한데(1.0만 대 1.0만) 안쪽의 무게가 다르다 — 704줄은 대부분
`fl.owner != tgt.id` 비교 하나로 끝나고(24.4ns), `_sweep` 은 **기체마다**
`v_sub`·`v_dot`×3·`v_add`·`v_mul`·`clamp` 에 튜플 생성까지다(769ns, **31배**).
`v_dot` 만 틱당 2만 번 불린다.

**그러면 앞 근무의 A/B(플레어 끄면 3배)는 무엇이었나.** 같은 A/B 를 세어
봤다(강제 연사 10초, 시드 7).

| | 동시 미사일 | 플레어 | `_sweep`/틱 | 704줄 반복/틱 |
|---|---|---|---|---|
| 플레어 켬(현재) | 533 | 348 | 304.8 | 14,268 |
| 플레어 끔 | 463 | 0 | 285.8 | 0 |

플레어를 꺼도 **`_sweep` 일감은 6%밖에 안 줄고**(305→286) 미사일 수는 13%
줄 뿐이다. 없어지는 것은 704줄 0.69ms 와 `_step_flares` 0.43ms — 합쳐서
1.1ms, 14.93ms 의 7.5%다. **3배가 나올 자리가 기계적으로 없다.** 관측 자체가 위의 벽시계
흔들림(같은 일에 1.5배, 최대 880ms 봉우리)에 잡힌 것으로 본다.

**고칠 곳은 `_sweep` 이다.** 비용은 `무장한 미사일 × 기체` 라 초선형이
아니라 **선형**이고, 상수가 크다(기체당 769ns). 볼 만한 것 둘:
· 넓은 단계 추리기 — 미사일 이동 선분의 AABB 밖 기체는 벡터 연산 전에 버린다
· 동시 미사일 수 자체에 상한(`_launch` 는 지금 무제한 `append` 한다)
둘 다 `game.py` 로직이라 프로그래밍실 소관이다.

**(2)·(3) 은 독립으로 확인했다.** 플레어가 스냅샷에서 상한이 없다는 것
(1111줄)은 그대로 맞다 — 강제 연사에서 플레어 11.7KB > 미사일 7.8KB(상한
186에 걸림, 월드에는 499발)였다. **덧붙일 것이 하나 있다: 24대 _자연_
교전에서도 이미 플레어(2,810B)가 미사일(2,610B)보다 크고, 스냅샷 최대가
10,291B 로 기준 10KB 를 이미 스친다**(중앙값 6,876B, 접속자당 168KB/s ·
최대 251KB/s). 강제 연사만의 이야기가 아니다.

측정 스크립트는 `_skyarena_probe/infra/probe6~10.py` 다. `probe7.py` 가
반복 횟수 세는 것, `probe10.py` 가 두 루프 미세측정이다.

## 새로 나온 것 — 봇 등급을 가르는 것은 사실상 유도탄 하나다 (2026-08-15, QA실)

`BOT_TIERS` 는 등급마다 조준오차·반응·당김·발사원뿔을 다르게 준다. 그런데
**그 네 값이 실제 승률을 거의 안 움직인다.** `game.py:218` 이 "실전 승률을
본 뒤 cone 까지 조정한다"고 적어 둔 그 승률을 재 봤다.

3대3 · 120초 · 시드를 바꿔 가며 붙이고 **상대 진영을 잡은 수만** 셌다
(같은 편 오사·지면 충돌은 뺐다). 누적:

| 맞대결 | 상대격추 | 승률 | p (이항, 양측) |
|---|---|---|---|
| veteran : rookie (기체 그대로) | 133 : 111 | 54.5% | 0.18 |
| veteran : rookie (둘 다 falcon) | 35 : 26 | 57.4% | 0.31 |
| **veteran : rookie 합계** | **168 : 137** | **55.1%** | **0.086** |
| veteran : ace (기체 그대로) | 84 : 65 | 56.4% | 0.14 |
| veteran : ace (둘 다 falcon) | 87 : 85 | **50.6%** | 0.94 |
| **veteran : ace 합계** | **171 : 150** | **53.3%** | **0.26** |
| **유도탄 있음 : 로켓만** (둘 다 veteran 파라미터·falcon) | **44 : 21** | **67.7%** | **0.0059** |

읽는 법은 이렇다.

- **유일하게 유의한 것은 유도탄 플래그다**(p=0.006). 같은 파라미터·같은
  기체인데 유도탄을 뺐더니 승률이 67.7% → 32.3% 로 뒤집힌다. 12판 중
  4판 전승이었다. **rookie 만 `missile: False` 다** — 등급 차의 실체가
  대체로 이것이다.
- **에이스는 베테랑보다 세다는 증거가 없다.** 기체를 같게 두면 50.6%,
  사실상 동전던지기다(p=0.94). 파라미터는 전부 '더 좋은' 쪽인데
  (err 0.045→0.018 · react 0.5→0.25 · gPull 0.6→0.75 · cone 0.055→0.035)
  결과가 안 따라온다. 난이도 `hard` 는 '에이스 다수'로 광고한다.
- **cone 은 방향이 반대로 먹을 수 있다.** cone 은 '이만큼 잘 겨눴을 때만
  쏜다'는 문턱이라 등급이 오를수록 **발사가 줄어든다.** 같은 조건 180초에
  rookie 2,324발 · veteran 1,054발 · ace 1,105발 — 루키가 2.2배 쏜다.
  탄약은 무제한이고 로켓은 46 피해다. 정확도로 얻는 것을 발사량으로 반납한다.

**확실히 해 둘 것 — 이 표는 "차이가 없다"를 증명하지 못한다.** 표본이
작아서 55% 안팎의 차이는 가려낼 힘이 없다. 유의하게 보려면 상대격추 표본이
rookie 쪽 약 400, ace 쪽 약 1,100 필요한데 지금은 305 · 321 이다.
**말할 수 있는 것은 "기대만큼 크지 않다"까지다.** 그래서 손대지 않았다 —
`BOT_TIERS` 를 돌리는 것은 난이도 설계라 사람이 정할 일이다.

**다음 사람에게 남기는 함정 하나.** `World._fill_with_bots` 는 매 틱 봇 수를
난이도 정원(normal=6)으로 되돌리고, 넘치면 **마지막에 넣은 봇부터** 지운다.
그래서 4대4 를 넣으면 뒤에 넣은 진영이 2대까지 깎여 **4대2 로 싸운다.**
여기서 실제로 한 번 속아 "루키가 베테랑을 91:57 로 이긴다"는 값을 얻었다 —
인원을 고정하고 다시 재니 뒤집혔다. 재현하려면 그 함수를 먼저 꺼라
(측정 스크립트는 저장소 밖 `_skyarena_probe/qa1/tiers2.py`·`tiers3.py`).

**CI 가 이걸 못 잡는 이유**도 같이 적어 둔다. `testpilot` 은 한 방에 섞인
봇으로 격추 총합만 본다. 같은 등급끼리 붙이면 격추 = 사망이라 **등급별
강약이 원리적으로 안 나온다.** 등급 비교는 진영을 갈라야만 보인다.

관련 파일: `server/game.py` (`BOT_TIERS`, `_bot_think` 의 `cone`·`missile`)

## 새로 나온 것 — node 가 깔려 있다. 클라 예측을 기계로 검사할 수 있다 (2026-08-15, 프로그래밍실)

`RESUME.md:11` 은 이렇게 못 박고 있다.

> node 가 깔려 있지 않으므로 JS 는 로컬 린트가 불가능하다 — 고친 뒤에는
> 브라우저에서 모듈 import 부팅 검사를 해야 한다.

**틀렸다. `node --version` 은 `v24.19.0` 이다.** 이 한 문장이 "클라 JS 는
사람이 브라우저로 봐야 한다"의 근거로 계속 인용돼 왔는데, 저장소 안에서
이미 `node --check` 를 쓴 기록이 여러 건 있는 것을 보면(이 파일 1016·1064·
1274줄) **사실은 진작 깔려 있었고 RESUME 만 안 고쳐진 것**이다.

이게 중요한 이유는 린트가 아니다. 이 프로젝트의 1순위 불변식인
**"서버 값 == world.js 값"을 사람 눈 대신 기계로 못 박을 수 있다**는 뜻이다.
`world.js` 는 순수 ES 모듈이고 `_integrate` 가 읽는 것은 `me`·`ac`·`cfg`
셋뿐이라, DOM 도 캔버스도 없이 node 에서 **원본 그대로** 부를 수 있다.

### 실측 — 200틱 맞대 보았다

`Plane.step` 과 `world.js _integrate` 에 같은 명령열(수직을 여러 번 넘기는
피치·롤·요 섞기)을 먹이고 `hpb`·`pos`·`vel` 을 틱마다 비교했다.

| 항목 | 최대 절대오차 |
|---|---|
| `hpb` 세 값 | **정확히 0** (200틱 전부) |
| `pos` · `vel` | 5.57e-12 (200틱) |

**공식은 같다.** 어긋나는 것은 노름 하나뿐이다 — 서버 `v_len` 은
`sqrt(x²+y²+z²)` 이고 클라 `v3.len` 은 `Math.hypot` 이다. 같은 속도벡터에서
둘의 차이가 1.14e-13 이고, 그것이 `spd` 를 타고 위치에 쌓인다.

| 틱 | 최대오차 |
|---|---|
| 200 | 5.6e-12 |
| 5,000 | 1.8e-11 |
| 20,000 (400초) | 6.5e-11 |

400초를 쉬지 않고 예측해야 0.065나노미터다. **고칠 이유가 없다** — 다만
`RESUME.md:102` 의 "hpb·pos·vel **비트 단위 동일**"은 `hpb` 에만 맞는 말이라
그 줄을 정확하게 고쳤다. `Math.hypot` 쪽이 더 정확한 구현이라 서버를
맞추는 방향이면 `v_len` 을 `math.hypot` 으로 바꾸는 것이지 반대가 아니다.

### 다음 사람에게

측정 스크립트는 저장소 밖 `_probe_server_parity/` 에 있다(`srv.py` 가
`Plane.step` 을 돌려 CFG·기체표째 JSON 으로 뱉고, `cli.mjs` 가 진짜
`world.js` 를 import 해 같은 명령열을 돌린 뒤 비교한다). 손으로 옮겨 적으면
'옮겨 적은 것'을 검사하게 되므로 **반드시 원본 모듈을 import 해야 한다.**

이걸 CI(`.github/workflows/ci.yml`)에 넣으면 "한쪽만 고쳐서 게임이 망가진다"
(위 규칙 5번)를 기계가 막는다. **넣을지는 사람이 정할 일이라 안 넣었다** —
CI 는 프로그래밍실 단독 소관이 아니다.

## 새로 나온 것 — 리드 마커의 수렴 루프가 아직도 헛돈다 (2026-08-15, UI실)

**번호를 안 붙였다** — 위 항목들과 같은 이유다(코드 주석이 순위를 인용한다).

오늘 조준경의 사거리 식을 하나로 합치다가(아래 '해결된 것') 바로 옆에서
나왔다. `hud.js` `gunsight()` 의 리드 마커 반복문은 **세 번을 돌아도 한 번
돈 것과 결과가 같다.** 2026-08-14 에 "예전에는 루프가 아무 일도 하지
않았다 … 이번에는 진짜로 수렴시킨다" 고 고쳐 적은 그 루프가, 방식만 바뀐
채 여전히 헛돌고 있다.

```js
aim = v3.add(me, v3.mul(dn, range));       // ← 반지름 range 인 구면 위로 되쏜다
t = flightTime(v3.len(v3.sub(aim, me)));   // ← 그래서 |aim − me| 는 항등적으로 range
```

`aim` 을 매번 `range` 구면에 되쏘므로 `|aim − me|` 가 **정의상 `range`** 다.
`t = flightTime(range)` 가 매 회차 같은 값으로 다시 계산되고, 세 회차의
조준 방향이 소수점 아래까지 똑같다. 실측(800m 앞 표적, 내 속도 830m/s,
표적이 옆으로 450m/s):

| 회차 | `\|aim − me\|` | `t` | 조준 방향 |
|---|---|---|---|
| 1 | 800.000000m | 0.652417s | [0.7505, 0.0053, 0.6608] |
| 2 | 800.000000m | 0.652417s | [0.7505, 0.0053, 0.6608] |
| 3 | 800.000000m | 0.652417s | [0.7505, 0.0053, 0.6608] |

**증상은 리드 각도다.** 비행시간을 '지금 거리'로 잡으므로 표적이 가로지를수록
어긋난다. 표적의 **미래 위치까지의 상대거리** `|rel + (tv − myVel)·t|` 로
수렴시키면 같은 표적에서 `t` 가 0.433s(**−50.6%**), 조준 방향이 **24.8도**
옮겨간다.

### 그런데 고치지 않았다 — 어느 모델이 옳은지 못 박지 못했다

서버 `_step_missiles` 를 그대로 적분해(항력·중력·추력 포함) HUD 가 가리키는
방향으로 실제 로켓을 쏘고 최근접 거리를 쟀다. 거리 6종 × 내 속도 3종 ×
기하 4종 × 표적 속도 2종 = **144 시나리오**.

| | 명중(신관 40m 안) | 평균 빗나감 |
|---|---|---|
| 지금 코드 | 73 / 144 | 110.7m |
| 미래위치 수렴 | **93 / 144** | **55.5m** |

평균은 확실히 낫지만 **한결같이 낫지는 않다.** 3,000m 옆 가로지름에서는
39m → 256m 로 오히려 나빠지고, 근거리(150m)·고속(1450m/s)에서는 조준 방향이
179.8도까지 뒤집히는 퇴화 사례가 나온다. 원인 후보가 둘이다.

1. **좌표계가 섞여 있다.** `flightTime()` 은 로켓이 *발사기 기준* 900m/s 로
   시작해 1000m/s² 로 가속한다고 본다. 그런데 서버는 가속을 **월드 속도
   방향**(`vhat`)으로 준다. 내 속도가 빠를수록 두 방향이 벌어져 상대계
   이동거리가 모델보다 작아진다 — 1450m/s 에서 퇴화가 몰리는 것과 맞는다.
2. **표적 등속 가정.** 비행시간이 1.3초를 넘는 3,000m 부근에서는 표적이
   실제로 선회하므로 어느 모델을 써도 크게 빗나간다.

**사람이 정할 것: 상대계 근사를 유지할 것인가, 월드계로 다시 세울 것인가.**
월드계로 세우면 항력까지 넣을 수 있어 정확하지만, 매 프레임 도는 코드라
비용을 재야 한다. 지시문이 "확신이 안 서면 고치지 말라"고 못 박아 두어
**동작은 그대로 두고 주석에 사실만 적어 두었다**(`hud.js` 의 ⚠ 표시).

검증 방법은 프로그래밍실이 세운 방식과 같다 — 손으로 옮겨 적지 말고 진짜
`hud.js` 를 `import` 해서 가짜 ctx 의 `fillText` 를 가로챈다.

### 곁다리 — '사거리 밖' 배지는 이제 도달 불가다

사거리를 바로잡고 나니(6,300m → 13,980m) 이 경고가 **뜰 수 없는 자리**가 됐다.
`range` 는 락온 표적까지의 거리인데 서버 락온 상한이 9,000m
(`WEAPONS[1]["range"]`)이고, 락온이 없으면 `range` 는 상수 800 이다. 즉
13,980m 를 넘길 경로가 없다.

`AOA 0.0°` · `RKT 99` 와 같은 부류로 보고 걷어낼 수도 있지만, 저 둘과 달리
**화면을 차지하지 않는다**(안 뜨면 아무것도 안 그린다). 락온 거리나 아레나
반경이 바뀌면 되살아나는 안전망이기도 하다. 지워도 되는지는 사람이 정할 일이라
그대로 두었다.

## 1순위 — 기체가 하얗게 뜬다 (노출 — 지형실 소관)

**진단이 끝났다(2026-08-14, 아트실).** '플라스틱처럼 보인다'의 네 후보 중
셋은 이미 해결돼 있었고, 남은 하나는 **아트실 파일 밖**이다.

헤드리스 실측(실제 `buildJet()` 메시를 소프트웨어 래스터라이즈해서 잼):

| 후보 | 판정 | 근거 |
|---|---|---|
| 면 단위 법선 | **아니다** | 동체 법선 곡률 중앙값 11.6도/m. 매끈하지 않다 |
| 재질 파라미터 | 해결됨 | metallic 0, 거칠기 0.62 + 절차적 편차 |
| 단색 알베도 | 해결됨 | 잡티를 전부 끄면 화면이 6.7 코드 RMS 움직인다 |
| 환경 반사 | **여기였다** | 아래 |

환경 반사(`shadePBR` 의 `env`)가 기체가 받는 빛의 **35%** 인데 거칠기 0.62
무광면에 **거울 표본**을 쓰고 있었다. 그래서 하늘에서 가장 밝은 자리(태양
둘레 미 후광)를 그대로 집어 표면이 떠 버렸다. 고쳤다 — '해결된 것' 참조.

**남은 진짜 원인은 노출이다.** 상면 도장 알베도는 선형 0.098(어두운 회색
FS36270)인데 화면에서 157 코드로 찍힌다. 수직미익은 205 코드다. 어두운
회색이 흰색으로 나오는 것이고, 이것이 "무광으로 만들어도 흰색은 흰색"의
정체다. 노출 곡선(`BASE_EXPOSURE` ≈ 7.7배)과 `PAINT` 표는 **둘 다
`scene.js` — 지형실 소관**이라 아트실이 손댈 수 없다.

- 아트실 수정으로 상면은 157 → 153, 동체 옆면 186 → 172 로 내려갔다.
  방향은 맞지만 노출을 안 건드리면 여기까지다.
- 지형실 1순위(노출)와 **같은 문제다.** 그 항목을 할 때 기체도 같이 볼 것.

관련 파일: `public/js/scene.js` (`BASE_EXPOSURE`, `PAINT`)

## 2순위 — 저공 속도감

순항 고도 3000m 에서는 속도가 안 느껴진다. 원인은 밝혀져 있다 — **지형 최단
파장이 662m 라 662m 보다 작은 지물이 원리적으로 없다.** 나무를 심어도
3000m 상공에서는 1픽셀이 안 된다. 스쳐 지나갈 것 자체가 없다.
(여기 적혀 있던 885m 는 x 축 성분만 본 값이었다 — 2026-08-15 지형실 정정.
아래 "885 → 662" 항목을 보라. 결론은 그대로다.)

막다른 길: 카메라 근처 공기 입자(`speedStreaks`)는 한 번 넣었다가 사용자가
싫다고 해서 걷어냈다. 속도 블러도 항상 최대로 걸려 화면이 흐리다는 지적을
여러 번 받아 완전히 껐다(`scene.js` 의 `const sBlur = 0`).

**지상 스캐터는 이미 만들어져 있고, 문턱대로 돈다** (2026-08-15 지형실이 실측).
이 항목이 "남은 후보"로 적어 두었던 것이 바로 그것인데 `scene.js:1817`
`groundScatter()` 로 이미 들어와 있다(HEAD 에 커밋된 코드다). 문서만 뒤처져
있었다. 다시 만들려고 하지 마라.

`groundScatter()` 는 `camEye`·`camQ` 만 읽는 순수 함수라 **한 장도 안 그리고**
직접 불러 문턱을 훑을 수 있다. 육지 한 점(x 1862 · z −834 · 지면고 177.9m)
위에서 카메라 AGL 만 바꿔 가며 `fx.transient` 호출을 셌다.

| 카메라 AGL | 높음(detail 3) | 보통(2) | (1) | 낮음(0) |
|---|---|---|---|---|
| 0 | 77개 / 알파합 45.0 | 50 / 26.2 | 14 / 5.4 | 0 |
| 150 | 78 / 48.3 | 51 / 29.4 | 15 / 7.7 | 0 |
| 330 | 78 / 48.3 | 51 / 29.4 | 15 / 7.7 | 0 |
| 400 | 78 / 35.8 | 51 / 21.8 | 15 / 5.7 | 0 |
| 500 | 78 / 17.9 | 51 / 10.9 | 15 / 2.8 | 0 |
| 599 | 78 / 0.18 | 51 / 0.11 | 15 / 0.03 | 0 |
| **600** | **0** | **0** | **0** | 0 |

읽는 법 — **개수는 AGL 과 무관하게 상수**이고(격자 크기로 고정, 주석이
약속한 대로다) 밝기만 움직인다. 알파는 AGL 330m 아래에서 평평하고
(`600 − 600×0.45 = 330`) 거기서 600m 까지 선형으로 빠져 599m 에서 0.18,
600m 에서 정확히 0 이다. **문턱에서 팝이 없다.** AGL 0 이 150 보다 오히려
옅은 것은 근접 페이드(30m 안쪽을 3차원 거리로 자른다)가 물어서다.

곁다리로 확인한 둘:
- 바다 위(지면고 −50m 이하)에서 AGL 100m 면 **0개**다. `gy < 12` 로 일부러
  건너뛴 것이라 정상이다 — 바다에서 안 보인다고 고장으로 읽지 마라.
- **자동 강등은 스캐터를 못 죽인다.** `AUTO_MAX_DEG` 가 4 이고 `DEGRADE[4]`
  의 `detail` 이 1 이라 15개가 남는다. 0개가 되는 것은 설정에서 품질
  '낮음' 을 **직접 고를 때**뿐이다(`PRESETS.low.detail = 0`).

**그래서 2순위에 남은 진짜 일감은 저공이 아니라 순항 고도다.** 스캐터는
설계대로 600m 아래에서만 살고, 첫 문장의 불만("순항 3000m 에서 안 느껴진다")은
그대로다. 그 높이에서 스쳐 갈 것을 만들려면 662m 벽을 깨야 하고, 그건
지형 프래그먼트에 고주파 한 겹을 얹는 일이라 **셰이더(아트실) 소관**이다 —
`terrain.js` 는 클립맵 격자와 프러스텀 컬링뿐이라 그 안에서는 할 수 없다.

**남은 둘도 쟀다 — 셋 다 문턱에서 팝이 없다** (2026-08-15 지형실).
앞 근무가 "그리기·갱신 루프 안이라 떼어 부를 수 없다" 고 남긴 둘이다. 실제로는
`Scene.prototype.updateFx` / `.drawGroundShadows` 를 프로토타입에서 떼어 **껍데기
this 에 `call`** 하면 GL 없이 돈다. 없는 속성을 물으면 no-op 을 돌려주는 Proxy
하나면 `gl`·`fx` 가 전부 대체된다. 스캐터와 같은 육지 점(x 1862 · z −834 ·
지면고 177.9m)에서 쟀다.

흙먼지 — 스폰 게이트가 `Math.random() < dt·sp/45·k` 라 확률이다. `Math.random`
을 0 으로 묶어 `k>0` 이면 반드시 쏘게 만들고 `fx.groundDust` 에 넘어오는 `k` 를
받아 적었다. 속도 300m/s:

| 기체 AGL | 0 | 50 | 100 | 130 | 200 | 259 | 259.9 | **260** |
|---|---|---|---|---|---|---|---|---|
| k(농도) | 1.000 | 0.808 | 0.615 | 0.500 | 0.231 | 0.0038 | 0.0004 | **0 (컷)** |

`k = 1 − agl/260` 정확히 선형이고 260m 에서 0 에 닿은 채 끊긴다. 곁가지 게이트
둘도 주석 그대로다 — 속도는 **150m/s 이상**(149 → 0개, 150 → 나온다), 카메라
거리는 **4000m 이하**(4000 → 나온다, 4001 → 0개).

접지 그림자 — `agl < -20` 에서 컷, 위로는 800m 컷. 농도 `(1−t)^1.4·0.62`:

| 기체 AGL | −25 | −20 | 0 | 100 | 200 | 400 | 600 | 799 | **800** | 801 |
|---|---|---|---|---|---|---|---|---|---|---|
| 반경 | — | 15.6m | 18.0m | 30.0m | 42.0m | 66.0m | 90.0m | 113.9m | 114.0m | — |
| 농도 | 0개 | 0.620 | 0.620 | 0.514 | 0.415 | 0.235 | 0.089 | 0.0001 | **0.0000** | 0개 |

**셋 다 문턱에서 정확히 0 에 닿고 나서 끊긴다.** 저공 효과에는 팝이 없다 —
이 각도로는 더 팔 것이 없다.

곁다리로 확인한 하나: `groundScatter` 의 `agl < 0` 조기 반환은 걸리면 지물이
77 → 0 으로 **한 프레임에 통째로 사라지는** 진짜 팝이다. 그런데 실전에서 안
걸린다. 지형 최대 경사가 0.626 이라 추격 카메라 거리 25m 동안 지면이 최대
15.6m 밖에 안 솟는데 카메라는 기체보다 6m 위에 있다 — 기체 AGL 이 **약 9.6m
아래**로 내려가야 비로소 음수가 된다. 육지 97,445 표본(기체 AGL 100m)에서 0회,
최저 카메라 AGL 93.5m. 손댈 필요 없다.

관련 파일: `public/js/scene.js`, `public/js/gfx/fx.js`

## 2순위 곁가지 — 885 → 662 정정 (2026-08-15, 지형실)

`terrainH` 의 최단 파장은 **662m** 다. 885m 이 아니다. 문서 여러 곳과 주석
세 곳이 885m 로 적혀 있었다.

최고 옥타브는 `A·sin(kx·x+px)·cos(kz·z+pz)` 인데, 곱-합 항등식
`sin(a)cos(b) = ½[sin(a+b)+sin(a−b)]` 로 풀면 파수벡터 `(kx, ±kz)` 짜리
**평면파 두 장**이다. 파장은 축 성분이 아니라 벡터 길이로 정해진다:

| 옥타브 | λx | λz | **λ(대각) = 진짜** |
|---|---|---|---|
| A=380 | 14960m | 16982m | 11225m |
| A=190 | 5280m | 6478m | 4093m |
| A=70 | 2087m | 2252m | 1531m |
| **A=22** | **885m** | 997m | **661.9m** |

885m 는 최고 옥타브의 **x 축 성분**이다. 어느 방향으로도 최단이 아니다 —
최단은 대각선 41.6도 방향의 662m 다. 수치로 세 갈래 확인했다: 항등식 잔차
4e-14 / 평면파 진행방향 실측 주기 661.94m(이론 661.94m) / 지형 최대 경사
0.626 ≤ 이론 상한 `Σ A·|k|` = 1.0005 (숨은 고주파가 없다는 뜻).

**결론은 하나도 안 바뀐다.** 662m 도 64m 셀에는 파장당 10.34 표본이라
나이퀴스트(2)의 5.2배다. 아레나 ±6km · 57,600 표본으로 잰 격자 재현 오차:

| 셀 | 64m | 128m | 256m | 512m | 1024m | 2048m |
|---|---|---|---|---|---|---|
| 표본/파장 | 10.34 | 5.17 | 2.59 | 1.29 | 0.65 | 0.32 |
| 최대오차 | **1.76m** | 6.93m | 24.9m | 67.7m | 150m | 262m |
| RMS | 0.49m | 1.90m | 6.67m | 18.6m | 45.1m | 84.2m |

바깥 두 링은 나이퀴스트 아래지만 32~65km 를 덮어 262m 오차가 시각 0.47도
(≈7px)이고, 링이 2×cell 로 스냅해 표본점이 월드에 고정되므로 헤엄치지 않는다.
지오메트리는 순수 시각물이라 판정에는 영향이 없다. **64m 6링은 정당하다.**

**고친 것:** `public/js/gfx/terrain.js` 머리 주석 (유도 과정과 실측표를 함께 적었다).

**아직 885m 인 채로 남은 곳 — 다음 사람 몫:**
- `public/js/m3d.js:164` (`terrainH` 위 주석) — **프로그래밍실/물리·수학 소관**이라
  지형실이 손대지 않았다. 원본에 해당하는 자리라 여기부터 고치는 게 맞다.
- `public/js/scene.js:54~56` (`저공 속도감` 머리 주석) — 지형실 소관이지만
  **이 파일에 다른 방의 미커밋 작업(노출/도장)이 얹혀 있어** 같이 커밋되는 것을
  피하려고 손대지 않았다. 그 작업이 커밋된 뒤에 한 줄만 고치면 된다.
  고칠 문구: "최고 주파수 파장이 885m 라 … 885m 짜리 언덕조차" → 662m.
- 위 2순위 본문은 이번에 고쳤다.

## 3순위 — 사람 눈이 필요한 것

이 환경은 브라우저 패널이 숨겨지면 rAF 가 멈춰 화면을 제대로 못 본다.
아래는 수치로만 확인했고 **실제 감각은 아무도 확인하지 않았다.** 사용자에게
직접 물어볼 것:

- 조준 리시 28도가 적당한지 (8도는 화면이 안 돌아 실패, 55도는 답답했다)
- 3인칭에서 기체가 화면에서 미끄러지는 느낌이 없는지
- 마우스 감도 cm/360 기본값 5 가 적당한지

## 4순위 — 스냅샷의 `st` 한 칸 (프로그래밍실 혼자서는 못 끝낸다)

UI실이 계기판을 정리하다 찾은 '서버가 죽은 값을 보낸다' 항목이다.
**네 칸은 2026-08-14 에 걷어냈다** — `aoa` · `bo` · `am` · `ms`
('해결된 것' 참조). 남은 것은 `st` 하나인데 읽는 곳이 전부 **다른 실
파일**이라 여기서 멈췄다.

서버는 `self.stalling` 을 `False` 로만 두고 아무도 `True` 로 바꾸지 않는다
(game.py:317·383). 옛 항공역학 분기의 잔재라 **영원히 0** 이다. 그런데도
매 틱 두 경로로 나간다 — `me.st`(game.py:1119)와 `p` 행 flags 의 8번
비트(game.py:1055).

| 읽는 곳 | 무엇 | 소관 |
|---|---|---|
| `scene.js:1714·1716` | 실속 와류 파티클 | 지형실 |
| `main.js:535` | 오디오로 넘기는 `stalling` | UI실 |
| `world.js:109·281` | 위 둘에 넘겨 주는 경유지 | 프로그래밍실 |

전부 **영원히 안 켜지는 이펙트**다. 지우려면 세 파일을 **같은 커밋에서**
고쳐야 한다 — 한쪽만 고쳐서 게임이 깨진 적이 있다(2026-08-13).

**사람이 정할 것: 실속을 되살릴 것인가, 이펙트째 지울 것인가.** 되살릴
거라면 `st` 는 그대로 두고 서버가 값을 실제로 움직이게 만드는 일이 된다.

### 곁다리 — 재장전 표시

**재장전까지 남은 시간**을 띄우려면 서버가 `p.fire_t` · `p.ms_cd` 를 스냅샷에
실어야 한다. 지금 스냅샷에는 쿨다운이 아예 없다. 두 칸 늘리는 값이고, 오면
계기판을 `∞` 에서 시간 막대로 바꾼다. 다만 로켓 쿨다운은 0.42초라 사람 눈에
뜻이 있는 것은 유도탄 0.9초 쪽뿐이다(`WEAPONS[…]["cd"]`, game.py:171·177).

## 5순위 — 죽은 코드 정리에서 넘긴 것 (2026-08-14, 프로그래밍실)

죽은 코드를 걷다가 **내 담당 파일 밖에서** 나온 것들이다. 고치지 않고 넘긴다.

> **2026-08-15 재확인.** 근무 지시문이 아직도 `flightmath.sound_speed` ·
> `q_from_basis` · `m3d.quat.integrate` · `quat.fromBasis` · `groundH` ·
> `airDensity` · `soundSpeed` · `CFG` 의 `gLimit`·`gLOC`·`stallWarnMargin` 을
> "지울 것"으로 지목하는데 **열 개 전부 이미 없다.** 지시문 2번의
> '추력/질량 15000' 주석도 `game.py:960`·`RESUME.md:141~144` 에서 이미
> 1000 으로 고쳐져 있다. 남은 것이 있나 싶어 두 파일의 심볼을 **전수 대조**
> 했다 — `flightmath.py` 정의 25개는 모두 호출부가 있고(최소 `q_integrate`
> 잔해 회전 1건 · `air_density` 미사일 항력 1건 · `q_axis_angle` `_look_q`
> 1건), `m3d.js` 도 `quat`·`m4`·`v3` 멤버와 최상위 export 전부 `m3d.js`
> 밖에서 쓰인다. **이 일감은 끝났다. 다음 근무는 여기서 다시 시작하지 마라.**

1. **`net.js` 의 기본 기종 `'striker'` 는 없는 키다.** 기종표는
   falcon · eagle · flanker 셋뿐이고 기본값은 `falcon` 이다(`game.py`
   `DEFAULT_AIRCRAFT`). 옛 기종표의 잔재다.
   - **증상은 없다.** `app.py:228` 이 `cls if cls in CLASSES else DEFAULT_CLASS`
     로 떨어뜨리고, 실제로는 `main.js:349` 가 사용자가 고른 기종을 넘기므로
     `'striker'` 가 서버까지 가는 경로 자체가 없다(재접속 경로도 그때는
     `this.cls` 가 이미 실제 기종이다).
   - 그래서 **급하지 않다.** 다만 읽는 사람이 "striker 라는 기종이 있나"
     하고 헤매게 만든다. `'falcon'` 으로 바꾸거나 아예 빈 문자열로 두면 된다.
   - `net.js` 는 프로그래밍실 담당 파일이 아니라 손대지 않았다.

2. **기체표에 옛 항공역학 값이 남아 있다** (`game.py` `AIRCRAFT`).
   `cl_a` · `cd0` · `k` · `wing` · `stall` 다섯은 **서버도 클라도 안 읽는다** —
   양력·항력·실속 분기가 사라질 때 계산식만 지우고 데이터는 남은 것이다.
   - **함정: 같은 표의 `mass` 와 `abThrust` 는 살아 있다.** `main.js:252` 가
     기체 선택 화면에 "추력 330kN · 자중 9.5t" 으로 찍는다. 표를 통째로
     정리하려 들면 선택 화면이 `undefined` 가 된다.
   - 이 표는 `welcome` 의 `classes` 로 클라이언트에 통째로 내려간다. 지우면
     스펙 표시를 하는 `main.js` 도 같이 봐야 하는데 그쪽은 다른 실 소관이다.
     **한 커밋에서 같이** 하거나, 아예 다음 사람이 판단할 일로 둔다.
   - 오늘은 손대지 않았다. 오늘 지운 것은 참조가 **0건**인 것들뿐이다.

3. **기체 설명문 셋 중 둘이 없는 물리를 약속한다** (2026-08-14 프로그래밍실
   발견, 안 고침). 주석 정리를 하다 나왔는데 이건 주석이 아니라 **사용자에게
   보이는 게임 문구**라 사람이 정할 일이다. `game.py` `AIRCRAFT` 의 `desc` 가
   `welcome` 으로 내려가 기체 선택 화면에 그대로 찍힌다(`main.js`).

   물리가 실제로 읽는 기체별 값은 **`pitchRate`·`rollRate`·`yawRate` 셋뿐**이다
   (game.py:363~365, 봇은 1030~1031). 속도와 가속은 `CFG` 의
   `arcMin`·`arcMax`·`arcBoost`·`arcAccel` 이라 **세 기종이 완전히 똑같다.**
   표의 `thrust`·`mass`·`abThrust` 를 읽는 곳은 서버에 0건이다(같은 이름의
   `WEAPONS[...]["thrust"]` 와 헷갈리지 마라 — 그쪽은 살아 있다).

   | 기종 | 설명문 | 판정 |
   |---|---|---|
   | falcon | "가볍고 롤이 빠르다. 근접전 최강." | **맞다** — rollRate 9.6 으로 최고 |
   | eagle | "추력이 압도적. 상승과 가속으로 싸운다." | **틀렸다** — 가속은 세 기종이 동일하다 |
   | flanker | "받음각을 크게 쓴다. 저속 선회가 강하다." | **틀렸다** — 받음각 모델이 없고 선회율은 속도와 무관하다 |

   flanker 가 '선회가 강한' 것 자체는 맞다(pitchRate 4.80 · yawRate 5.0 으로
   최고). 틀린 것은 **'받음각'과 '저속'** 두 낱말이다.
   - 고르는 길은 둘이다. (a) 설명문을 실제 차이(회전율)로 고쳐 쓴다 —
     문구만 바꾸면 되고 코드는 안 건드린다. (b) 기종별 속도·가속을 진짜로
     넣어 설명문을 사실로 만든다 — 이건 `CFG` 를 기종별로 쪼개는 물리 변경이라
     world.js 예측과 봇까지 같은 커밋에서 봐야 한다. **(a) 를 권한다.**
   - 어느 쪽이든 게임 밸런스·문구 결정이라 지시 없이 손대지 않았다.

4. **`#ammo` CSS 규칙이 붙을 요소가 없다** (`public/css/style.css:229~231`,
   UI실 소관). `#ammo` 와 `#ammo i` · `#ammo i.on` 세 줄인데 `index.html`
   에도 `public/js` 어디에도 `ammo` 라는 id 를 만드는 곳이 **0건**이다.
   계기판이 캔버스로 넘어가면서 남은 옛 DOM 계기판의 잔재로 보인다.
   확인: `grep -rn ammo public/` 이 이 세 줄만 잡는다.

5. **`hud.js:645` 주석이 이제 사실과 다르다** (UI실 소관). "서버가 `v.aoa`
   를 0.0 으로 두고 한 번도 바꾸지 않아"라고 적혀 있는데, 오늘 `v.aoa` 는
   **정의째 사라졌다**(위 '해결된 것'). 이어지는 "서버가 실제 받음각을
   보내기 시작하면 그때 되살린다"도 이제 칸을 새로 만드는 일이 됐다.
   AOA 칸을 뺀 판단 자체는 그대로 옳다 — 주석 두 줄만 손보면 된다.

6. **계기판이 또 없는 규칙을 지어내고 있다 — 이번엔 G 다** (2026-08-14
   발견). **HUD 쪽 (a) 는 2026-08-15 UI실이 끝냈다**('해결된 것' 참조) —
   `hud.js` 의 호박색(7)과 `OVER G`(8.5)를 걷어냈다. **이제 하나 남았다.**

   | 어디 | 무슨 말 | 실제 | 상태 |
   |---|---|---|---|
   | 기체 선택 화면 `main.js:254` | "30G" (`gLimit`) | 아무 데서도 강제되지 않는다 | **남았다** |
   | 서버 주석 `game.py:398~402` | "HUD 는 7 에서 호박색, 8.5 에서 OVER G" | 이제 둘 다 없다 | 고침(2026-08-15 프로그래밍실) |
   | HUD `hud.js` 호박색·`OVER G` | 결과 없는 경고 | — | 걷어냄 |

   서버의 G 값은 `game.py` `Plane.step` 의 `gload = 1.0 + abs(pr) * 4.0`
   하나뿐이고 **표시 전용**이다. 이 식의 최대값은 flanker 20.2 · falcon 18.2 ·
   eagle 15.2 로 **선택 화면이 약속한 30G 에 닿을 수조차 없다**(flanker 의
   gLimit 은 28인데 그마저 못 닿는다). 즉 사용자는 "30G 짜리 기체"를 골라서
   아무 일도 겪지 않는다.
   - **남은 길은 (c)**: 선택 화면에서 `gLimit` 칸만 뺀다. `main.js` 는 오늘
     UI실에 배정된 세 파일(`hud.js`·`input.js`·`index.html`) 밖이라 안 건드렸다.
   - (b) `gLimit` 을 실제로 강제한다(초과 시 피해)는 **권하지 않는다** — 물리
     변경이라 world.js 예측과 봇까지 같은 커밋이어야 하고, 아케이드 비행
     모델에 G 한계를 되살리는 건 `RESUME.md` 가 지운 것을 되돌리는 일이다.
   - 서버 쪽 `gload` 자체를 지우면 스냅샷 칸이 바뀐다. **지우지 마라** —
     `scene.js:1705` 가 이 값으로 와류를 띄우고 계기판도 숫자는 계속 쓴다.
     `game.py:398~402` 는 **주석만** 고치면 된다.

7. **HUD 의 마하가 고도를 안 본다** (2026-08-14 발견, 안 고침).
   `game.py` `Plane.step` 이 `self.mach = spd / 340.0` 으로 음속을 해면
   값에 고정한다. 고도별 음속을 주던 `flightmath.sound_speed` 는 항공역학
   분기와 함께 지웠는데, **HUD 속도 테이프(`hud.js:60`)는 여전히 이 값을
   `M x.xx` 로 찍는다.**
   - 오차: 출격 고도 3200m 에서 **+3.7%**, 상승한계 17000m 에서 **+15.2%**
     (표준대기 기준 음속 327.8 / 295.1 m/s).
   - 이건 위 6번과 달리 **서버 파일 안에서 혼자 고칠 수 있다** — `sound_speed`
     를 되살려 `spd / sound_speed(pos[1])` 로 두면 끝이고, 스냅샷 형식도
     안 바뀐다(`mach` 칸 그대로). 물리에는 전혀 영향이 없다(표시 전용).
   - 안 고친 이유는 **숫자가 화면에서 바뀌는 변경**이라서다. 마하는 사용자가
     읽는 값이고, 지금 값에 익숙해져 있을 수 있다. 사람이 정할 일로 둔다.

8. **`RESUME.md` '남은 일' 두 번째 항목이 이미 지난 일이다** (문서 결함).
   "봇 조종기는 아직 옛 뱅크투턴이다. `BOT_CHART_AXIS` 를 `True` 로 바꾸면
   … **일부러 꺼 두었다**" 라고 적혀 있는데, `game.py:197` 은 이미
   `BOT_CHART_AXIS = True` 다. 같은 문서 앞쪽(`봇에도 같이 적용한다`
   절)에는 "`BOT_CHART_AXIS` 는 이제 `True` 다" 라고 **반대로** 적혀 있어
   문서가 스스로와 어긋난다.
   - `RESUME.md` 는 다음 사람이 **제일 먼저 읽는** 문서라 이 한 줄이
     "봇은 아직 옛 제어기"라는 잘못된 전제를 심는다.
   - 고칠 것은 '남은 일' 쪽 한 항목이다. 다만 함께 적힌 (a)~(c)
     — 55도 리시가 답답한지 · 외부 시점 미끄러짐 · cm/360 기본값 12 —
     는 **아직 사용자 확인이 안 됐으므로 그대로 남겨야 한다.**
   - 이번 범위가 코드 파일이라 `RESUME.md` 는 손대지 않았다.

## 6순위 — 소리 (2026-08-14, 사운드실)

지시문의 '오늘 할 일' 네 가지가 **전부 끝났다**(2026-08-15). 이 절에 남은
것은 아래 곁다리 둘과 죽은 코드 목록뿐이다.

| 지시문 항목 | 실제 상태 |
|---|---|
| 1. 엔진음 | **했다**(2026-08-15) — 속도를 음높이로 넣었다. '해결된 것' 참조 |
| 2. 바람 소리 | **했다**(2026-08-14) — '해결된 것' 참조 |
| 3. 무기음 | **했다**(2026-08-14) — '해결된 것' 참조 |
| 4. 경고음 | **했다**(2026-08-15) — 미사일 접근(RWR)은 진작 울고 있었고(`world.js:151`), 비어 있던 지면 접근 경고를 넣었다. '해결된 것' 참조 |

> **함정(다음 사람도 걸린다):** `ev.k` 는 이벤트마다 뜻이 다르다.
> `launch` 에서는 **무기 종류**, `kill` 에서는 **격추한 사람 이름**이다
> (`main.js:473` 이 그렇게 읽는다). 한 이름으로 두 가지를 보내고 있다.

**부르는 곳이 0건인 소리** — 2D 판(`legacy-2d`)의 잔재로 보인다. 죽은 코드
정리를 할 사람에게 넘긴다: `pickup()` · `levelup()` · `roll()` ·
`boost()`/`stopBoost()`. (`boost` 는 `setEnabled` 안에서만 불린다.)
`fire()` 는 목록에서 뺐다 — 3번을 하면서 `rocket()` 으로 바꿔 썼다.

### 곁다리 — 발사음에 거리가 없다 (서버 쪽 값이 필요하다)

`launch` 이벤트는 **누가 쐈는지(`id`)만** 있고 **어디서 쐈는지가 없다.**
그래서 지도 반대편에서 난 발사도 내 코앞에서 난 것과 똑같은 크기로 들린다.
24인 방에 봇까지 있으면 발사음이 계속 깔린다. 지금은 `_throttle` 로
겹치는 것만 막아 뒀는데(로켓 70ms · 유도탄 130ms), 이건 **전체 공용 문턱**이라
남이 쏜 직후 내가 쏘면 내 소리가 먹힌다. 제대로 하려면 둘 중 하나다.

- 서버가 `launch` 에 발사 지점(`x`·`y`·`z`)을 실어 준다 — 프로그래밍실 소관.
- 또는 클라가 `id` 로 스냅샷에서 그 기체 위치를 찾아 쓴다. 봇·원거리 기체가
  스냅샷에 늘 들어 있는지 확인이 필요하다(5순위 죽은 칸 정리와 겹쳐 볼 것).

거리를 얻으면 `1/(1+d/800)` 정도로 게인을 깎고, 내 기체가 쏜 것만 원래
크기로 두면 된다. **소리 자체는 안 건드려도 된다 — 게인만 곱하면 된다.**

## 7순위 — 콕핏을 제대로 만들면 성능 예산을 넘는다 (2026-08-14, 아트실)

지시문 '오늘 할 일' 1번(조종석이 상자다)을 만들었고 **모양은 다 됐는데
성능 검증에서 걸렸다. 그래서 커밋하지 않았다.** 사람이 판단할 일이다.

> **그 코드는 지금 `public/js/gfx/jet.js` 의 커밋 안 된 작업 트리 변경으로만
> 남아 있다**(291줄). 커밋도 스태시도 안 된 상태라 `git checkout` 한 번이면
> 사라진다. 2026-08-14 아트실(마이크로 범프)은 이것을 건드리지 않으려고
> `shaders.js` 만 경로를 찍어 커밋했다. 사람이 살릴지 버릴지 정할 때까지
> **작업 트리를 청소하지 마라.**

**먼저, 예전 콕핏이 왜 '회색 덩어리'였는지가 실측으로 나왔다.**
눈은 기체축 (0, 1.30, 4.20)이고 세로 화각은 75도다(속도가 붙으면 101도).
그래서 눈앞 d 미터에서 **화면 아래 끝은 y = 1.30 - 0.767 d** 다.

| 예전 조각 | 위치 | 정면 75도에서 화면 점유 |
|---|---|---|
| 계기 코밍 | y 0.32~0.52, z 4.26~4.94 | **0.00%** — 한 번도 나온 적 없다 |
| 캐노피 보우 | y 0.96~1.07, z 4.89~5.01 | 7.95% |
| 사이드 레일 ×2 | y 0.45~1.05 | 3.61% |
| 기수 끝단 | z 6.30~8.90 | 0.66% |

즉 화면에 나오던 것은 **막대 세 개와 기수뿐**이었다. 계기 코밍은 화면 아래
끝보다 0.47m 낮아 원리적으로 도달 불가였다 — 계기판의 AOA 칸과 같은 부류다.

또 하나: **캐노피 밑동(y≈0.59)이 눈보다 0.71m 아래다.** 실제 전투기는 눈이
캐노피 레일 위 0.2m 남짓이다. 밑동에 맞춰 콕핏을 지으면 통째로 화면 밖이
된다. 만든 것은 구조물만 0.42m 들어 올려 피했지만 **근본 해결은 눈을
내리는 것이고 그건 `scene.js` camera() — 지형실 소관**이다.

**막힌 곳은 성능이다.** 콕핏은 깊이를 비우고 `FS_PBR` 로 다시 그리므로
비용이 화면 점유율에 그대로 비례한다. 그런데 **보이는 콕핏은 화면을 많이
덮을 수밖에 없다** — 그게 보인다는 뜻이다.

실측(1920×1080, Intel Iris Xe / ANGLE D3D11, 실제 FS_PBR·실제 유니폼):

| 조건 | 이전 | 이후 | 차이 |
|---|---|---|---|
| 화각 75 · 정면 | 1.00ms | 2.55ms | **+1.55ms** |
| 화각 101 · 정면 | 0.83ms | 2.13ms | +1.30ms |
| 화각 75 · 28도 아래 | 1.52ms | 3.61ms | +2.09ms |
| 화각 101 · 28도 아래 | 1.13ms | 3.27ms | **+2.14ms** |

지시문 예산은 **최악 +0.15ms** 다. 14배다.

이미 짜낸 것(이 수치는 짜낸 **뒤**의 값이다):
- 그리는 순서를 '가리는 것부터'로 바꿨다. 깊이를 비우고 그리므로 툽 벽을
  먼저 내면 화면을 채운 뒤 전부 덮인다. 최악 3.77 → 2.14ms.
- 앞 격벽을 y 0.28~0.96 → 0.26~0.47 로 줄였다. 그 판 하나가 정면에서
  **0.95ms** 로 콕핏 최대 지출이었다(계기판에 다 가리면서 화면은 채운다).
- 사이드 레일을 얇게 했다(반폭 0.075 → 0.05).

**남은 길은 셋뿐이고 전부 사람이 정할 일이다.**
1. 예산을 올린다. 콕핏 시점은 기본이 아니고(3인칭이 기본) 설정에서 끌 수도
   있다(`settings.cockpit`). **다만 예전 콕핏도 이미 1.0ms 를 쓴다** — 즉
   +0.15ms 로는 어떤 콕핏도 못 만든다. 예산이 이 항목과 맞지 않는다.
2. 콕핏 전용 싼 경로를 판다. `FS_PBR` 에 콕핏 부품 코드를 하나 더 만들어
   `aerial()`(눈에서 1m 안이라 무의미) · `cloudShadow()`(1m 안에서 상수) ·
   패널라인 블록을 건너뛴다. **아트실 파일 안에서 가능하다**(shaders.js +
   jet.js). 얼마나 줄지는 안 재 봤다 — 지배적인 비용은 `skyRadiance` 인데
   그건 콕핏이 빛을 받는 근거라 못 뺀다. 기대치는 낮다.
3. 안 만든다. 그러면 1번 항목을 지시문에서 빼야 한다 — 다음 사람이 또
   같은 벽에 부딪힌다.

만든 것은 검증을 **성능 빼고 전부** 통과했다(커밋 안 함, 되살리려면 이
항목을 보라): 948 삼각형(이전 84), `buildJet` 메시는 리팩터 전후 비트 단위
동일, 닫힌 조각 49개 전부 법선이 바깥, NaN 0건, aFlex 전부 0, pbr 6변형
(기본/SKIN/SKIN+DIRECT_OUT × highp/mediump) 컴파일·링크 통과, GL 오류 0건,
실제 서버로 띄운 게임 페이지 콘솔 오류 0건. 계기판·MFD 3대·글레어실드·
윈드스크린 보우가 정면 75도에서 화면의 29.5% 를 덮는다(이전 12.2%).

## 8순위 — 무장 칸 `s.w` 이 이제 아무도 안 읽는다 (2026-08-15, UI실이 넘김)

무장 선택 표시를 걷어내면서(아래 '해결된 것') **클라이언트에서 `s.w` 를 읽는
곳이 0건**이 됐다. 4순위의 `aoa`·`bo`·`am`·`ms` 와 같은 부류인데, 이쪽은
읽는 곳만 죽은 게 아니라 **왕복 전체가 죽었다.**

| 어디 | 무엇 | 소관 |
|---|---|---|
| `input.js` | `this.weapon` — 이제 **상수 0** 이다 | UI실(했음) |
| `main.js:589` | 입력 패킷의 `w: cmd.weapon` | 다른 실 |
| `app.py:303` | `p.weapon = w` | 프로그래밍실 |
| `game.py:285·1144` | `Plane.weapon` 과 스냅샷의 `"w"` | 프로그래밍실 |

`_launch()` 는 `p.weapon` 을 **아예 보지 않는다**(game.py:509·511 이 버튼으로
가른다). 즉 이 값은 매 틱 클라 → 서버 → 클라를 한 바퀴 돌고 버려진다.
지우려면 네 파일을 **같은 커밋에서** 고쳐야 한다 — 한쪽만 고쳐서 게임이
깨진 적이 있다(2026-08-13).

**지금은 형식만 유지해 뒀다.** `input.js` 의 `weapon` 을 상수 0 으로 두어
서버가 받는 패킷 모양은 그대로다(실측: 입력 패킷 15개 전부 `w` 칸이 있고
값은 전부 0). 그래서 **급하지 않다** — 지워도 안 지워도 게임은 같다.

## 9순위 — 원뿔 각도 1.15rad 이 세 곳에 손으로 베껴져 있다 (2026-08-15, UI실이 넘김)

기총 원뿔의 반각은 서버 `game.py:585` 의 `aim_dir(self, p, max_off=1.15)`
하나가 정본인데, **welcome 이 이 값을 안 내려보낸다**(`app.py:243` 의
`cfg`=CFG·`classes`=CLASSES 뿐이고 CFG 에 `max_off` 가 없다). 그래서 같은
숫자가 세 곳에 손으로 적혀 있다.

| 어디 | 무엇 | 소관 |
|---|---|---|
| `game.py:585` | `max_off=1.15` — **정본** | 프로그래밍실 |
| `hud.js:252` | `const outside = aimOff > 1.15` | UI실 |
| `index.html:84` | 도움말의 '기수에서 66도' | UI실(오늘 대조함) |

`hud.js:436` 의 `lockTime` 이 정확히 이 방식으로 조용히 어긋난 적이 있다
(클라가 0.9 로 박아둬서 락온 링이 20%까지만 찼다). 지금은 셋 다 맞지만,
서버에서 `max_off` 를 바꾸면 **HUD 는 아무 말 없이 거짓말을 시작한다** —
주황 X 가 안 뜨는데 탄은 원뿔 경계로 나가는 상태다.

- 고치는 길은 `lockTime` 때와 같다: CFG 에 `max_off` 를 넣어 welcome 으로
  내리고, `hud.js` 가 `W.cfg.maxOff` 를 읽게 한다. **`app.py`·`game.py` 를
  같이 고쳐야 하므로 UI실 혼자서는 못 끝낸다.**
- **덤 — 서버 독스트링이 이미 틀렸다.** `game.py:587` 이 "클라이언트 리시
  기본값 55도 + 여유 11도"라고 적어 두는데 리시 기본값은 **28도**다
  (`main.js:70`, `leashv` 3판). 55 는 두 판 전 값이다. 숫자를 옮길 때
  같이 안 옮긴 자리가 여기 하나 더 있다.

## 10순위 — 칸 수 검사가 실제로는 미사일만 본다 (2026-08-15, 프로그래밍실)

`ci.yml` 의 "서버가 보내는 칸 수와 클라가 읽는 칸 수가 맞는가" 는
**2026-08-13 에 게임이 망가진 바로 그 자리를 막으려고** 만든 검사다. 그런데
셋 중 **미사일 하나만 실제로 검사하고 있다.** 플레어와 레이더는 매번 조용히
건너뛴다 — 검사 안에 이 두 줄이 있다.

```
rows = snap.get(key) or []
if not rows:
    continue          # ← 비어 있으면 '통과' 로 지나간다
```

검사 시나리오가 `want_fire` 와 `want_missile` 만 켜기 때문에 벌어지는 일이다.

- **플레어** — 아무도 `want_flare` 를 안 켜서 `self.flares` 가 끝까지 빈
  배열이다.
- **레이더** — `rd` 는 `viewR`(14000m) **밖** 기체만 담는데, 아레나 반경이
  4500m 라 기본 시야 안에 전원이 들어온다. 즉 이 시나리오에서는 원리적으로
  한 줄도 안 생긴다.

그래서 지금 CI 는 **플레어 7칸·레이더 4칸이 어긋나도 초록불을 준다.**
`fl` 은 2026-08-14 에 3칸에서 7칸으로 늘어난 적이 있는 칸이라(속도 3칸 추가,
`game.py:1106~1113` 주석) 실제로 움직이는 자리다.

오늘 손으로 채워서 재 봤다 — **지금은 셋 다 맞다.**

| 배열 | 줄 수 | 서버 | 클라(`world.js`) | 판정 |
|---|---|---|---|---|
| `m` (미사일) | 105 | 8칸 | 8칸 (`world.js:120`) | 맞다 |
| `fl` (플레어) | 104 | 7칸 (`game.py:1111`) | 7칸 (`world.js:123`) | 맞다 |
| `rd` (레이더) | 3 | 4칸 (`game.py:1075`) | 4칸 (`world.js:125`) | 맞다 |

**재현 방법** — 검사 시나리오에 두 가지를 더하면 셋 다 채워진다.

- 플레어: 루프 안에서 `p.want_flare = True` 도 같이 켠다.
- 레이더: `w.snapshot(viewer, {}, view_r=1.0)` 처럼 시야를 좁혀 부른다.
  (`snapshot` 이 이미 `view_r` 인자를 받는다 — `game.py:1066`.)
  단 시야를 좁히면 `near()` 가 미사일·플레어를 걷어내므로, **`rd` 는 좁힌
  스냅샷에서·`m`/`fl` 은 기본 스냅샷에서** 따로 봐야 한다. 한 번의 호출로
  셋을 다 채울 수는 없다.

`.github/workflows/ci.yml` 은 프로그래밍실 담당 다섯 파일 밖이라 **손대지
않았다.** 고치는 값은 크다 — 지금은 없는 검사를 있다고 믿고 있는 상태다.

---

## 해결된 것 (되살아나지 않게 기록)

- **스냅샷의 `me.de` 를 뺐다** (2026-08-15, 프로그래밍실) — 위 QA실 항목의
  곁다리다. 서버가 매 스냅샷 사망 수를 실어 보내는데 **클라이언트 전체에서
  `me.de` 를 읽는 곳이 0건**이었다.

  화면의 K/D 는 안 바뀐다. 리더보드 행이 사망 수를 **따로** 싣고
  (`leaderboard()` 의 여섯 번째 칸 `p.deaths`) `main.js:466` 이 그 행을
  구조분해해 `${kl}/${de}` 로 찍는다 — 스냅샷과 완전히 다른 경로다.
  `v.deaths` 자체는 남겨 뒀다: 리더보드와 재접속 복원(`app.py:202`·`213`)이
  쓴다.

  | | 고치기 전 | 고친 뒤 |
  |---|---|---|
  | `me` 사전 | 32칸 · 361 B | 31칸 · **354 B** |
  | 사람당 | — | **−175 B/s** (25Hz) |
  | 24인 만원 방 | — | **−4.1 KB/s** |

  **`ci.yml` 의 `EXPECT` 키 집합도 같이 고쳤다.** 그 검사는 `me` 를 이름표
  집합으로 못 박아 두어서(칸 수로는 어긋남이 안 잡힌다 — 서버가 칸을 빼면
  클라는 `undefined` 를 읽고 조용히 `NaN` 을 찍는다), **의도적으로 칸을 뺄
  때 이 목록을 같이 고치는 것이 그 검사의 사용법**이다. 고치면서 클라를 다시
  판 결과가 위 '0건'이다.

  검증: `py_compile` 통과 · 봇 60초(격추 12 = 사망 12, 자체 기울기 0.0도,
  기수각 180도, 생존 6대) · 칸 수 정합 `m` 8 · `fl` 7 · `rd` 4 · `p` 10 ·
  `me` 31 전부 일치.

  **`me.w` 는 안 건드렸다** — 8순위 결정 전에 빼면 되돌리는 일이 된다.

- **조준경이 로켓 사거리를 두 가지로 계산하고 있었다** (2026-08-15, UI실) —
  `hud.js` `gunsight()` 안에서 같은 값을 두 식이 따로 구했다. SHOOT 판정은
  등가속 해(13,980m)를, '사거리 밖' 배지는 옛 등속 해(`muzzle * life` =
  6,300m)를 썼다. 2026-08-14 에 등속 가정을 걷어내면서 **SHOOT 쪽만 고치고
  배지를 안 따라 고친** 자리다. 주석에는 "짧게 잡으면 맞는 거리인데도
  '사거리 밖'이 떠서 쏘기를 망설이게 된다" 고 증상까지 적혀 있었는데, 정작
  그 증상을 내는 줄은 그대로 남아 있었다.

  **화면이 스스로와 모순됐다.** 진짜 `hud.js` 를 import 해 가짜 ctx 의
  `fillText` 를 가로채 재 보면 이렇다.

  | 락온 거리 | 고치기 전 | 고친 뒤 |
  |---|---|---|
  | 6,299m | SHOOT | SHOOT |
  | 6,301m | **SHOOT + 사거리 밖(동시)** | SHOOT |
  | 9,000m (락온 상한) | **SHOOT + 사거리 밖(동시)** | SHOOT |
  | 13,000m | **SHOOT + 사거리 밖(동시)** | SHOOT |
  | 14,500m | 사거리 밖 | 사거리 밖 |

  **어느 쪽이 맞는지는 서버로 재서 정했다.** `_step_missiles` 를 그대로
  적분(추력·항력·중력)해 무유도 로켓의 실제 도달 거리를 쟀다.

  | 발사 기체 속도 | 실제 도달 | 소진 시 속도 |
  |---|---|---|
  | 300 m/s | 13,448m | 1,676 m/s |
  | 450 m/s (출격 속도) | **14,177m** | 1,746 m/s |
  | 830 m/s | 15,960m | 1,910 m/s |
  | 1450 m/s (부스터) | 18,692m | 2,148 m/s |

  등가속 식(13,980m)이 출격 속도 기준 실측의 **−1.4%** 다 — 항력을 빼는
  오차와 발사기 속도를 빼는 오차가 서로 상쇄된다. 등속 식은 2.2배 짧았다.
  빠를수록 보수적으로 어긋나는데, '못 닿는다'고 말하는 쪽은 그게 안전하다.

  **손으로 베낀 폴백 세 개도 같이 걷어냈다.** `muzzle ?? 1030` 과 사거리
  `2880` 두 곳인데, 셋 다 지금 서버 어디에도 없는 숫자다(실제 900 · 13,980).
  welcome 이 오기 전 한두 프레임 동안 이 값들이 쓰여서, 제원도 모르는 채
  **리드 마커를 그리고 '사거리 밖'까지 띄우고 있었다**(실측: `weapons` 가 빈
  배열일 때 7,000m 표적에 `여기를 겨냥 / 사거리 밖` 이 떴다). 이제 제원이
  없으면 둘 다 건너뛴다 — 거리 숫자만 찍는다. `targets()` 의 `lockTime` 이
  똑같이 베껴 적어 어긋났던 전례가 있다(9순위와 같은 부류).

  검증: `node --check` hud.js·input.js 통과. 헤드리스 크롬으로 실제 서버에
  붙어 판을 시작(`flying` · `me` · `weapons` 2칸 확인) — **콘솔 오류 0건**,
  계기판 `fillText` 정상. 남은 리드 루프 문제는 위 '새로 나온 것' 으로 넘겼다.

- **엔진음이 속도를 안 듣고 있었다** (2026-08-15, 사운드실) — 6순위 1번, 지시문
  '오늘 할 일'의 마지막 항목. `engine()` 은 스로틀과 애프터버너만 읽었다.
  `main.js` 가 바로 옆줄에서 `wind()` 에 서버 속도를 넘기고 있었으므로
  `sfx.engine(cmd.throttle, cmd.ab, world.srv.sp)` 로 한 칸 더 넘기면 됐다.

  **세기가 아니라 음높이에 붙였다.** 바람이 이미 속도를 세기로 말하고 있어서
  엔진까지 세기로 붙이면 둘이 같은 말을 한다. 대역(`bp`)과 오실레이터(`osc`)에
  **같은 배율**을 곱한다 — 압축기 소음은 배음이 RPM 에 통째로 실리므로 한쪽만
  옮기면 한 엔진에서 두 소리가 난다. 눈금은 서버 CFG 그대로다(`arcMin` 300 ·
  `arcMax` 830 · `arcBoost` 1450, 제동은 목표 ×0.55 라 최저 165).
  **300 을 배율 1.0 으로 잡아 스로틀 0 순항의 소리는 예전과 완전히 같다.**

  | 서버 속도 | 배율(이론) | 톱니 기본파(실측) | 실측/이론 | RMS |
  |---|---|---|---|---|
  | 165 (제동) | 0.9648 | 85.92 Hz | 1.0006 | ×0.981 |
  | 300 (스로틀 0) | 1.0000 | 89.00 Hz | 1.0000 | ×1.000 |
  | 830 (스로틀 100%) | 1.1383 | 101.36 Hz | 1.0005 | ×1.060 |
  | 1450 (AB) | 1.3000 | 115.67 Hz | 0.9998 | ×1.124 |

  스로틀 0 · 100% · AB 세 곳에서 300→1450 배율이 각각 **1.2993 · 1.3000 ·
  1.3000** 으로 나왔다 — 자세나 스로틀과 무관하게 같은 배율이다. 폭은 4.6반음
  이고 스로틀이 혼자 옮기는 폭(52→126Hz, 15반음)의 3분의 1 이라 음높이의
  주인은 여전히 스로틀이다.

  **속도가 스로틀과 다른 말을 하는 자리는 셋뿐이다.** 서버 속도는 스로틀이 정한
  목표로 초당 700 씩 다가가는 값이라(`Plane.step`) 그 셋을 빼면 스로틀의 지연
  복사본이고, 이 소리가 새로 알려 주는 것도 딱 그 셋이다 — (a) 레버를 밀어
  넣는 0.76초(AB 1.64초), (b) **제동**(`engine()` 은 브레이크를 받지도 않으니
  속도만이 안다), (c) 상승한계 위(서버가 속도를 지운다, 맨 위 항목).
  레버를 끝까지 민 뒤 최종 음높이의 95% 에 닿기까지 **옛 코드 0.2초 → 새 코드
  0.5초**, 최종값 126.0 → 142.0Hz 다. 엔진이 물려 올라가는 느낌이 여기서 난다.

  검증은 **진짜 Chrome 의 Web Audio 로 렌더링해서** 쟀다(마스터 1.0, 잡음
  버퍼는 `Math.random` 을 고정해 실행마다 동일). 위 표가 그 결과다. 그 밖에:
  - **속도 300 을 주면 옛 코드와 결과가 완전히 같다** — 스로틀 0 · 0.5 ·
    AB 세 경우 모두 기본파·RMS·최대진폭·무게중심이 **전부 일치**했다.
    `git show HEAD:public/js/audio.js` 를 같은 하네스에 나란히 물려 A/B 했다.
  - 값이 없거나 숫자가 아닐 때(`undefined`·`null`·`NaN`·문자열·`Infinity`)
    다섯 경우 모두 300 과 결과가 일치한다 — **모르면 예전 소리**다.
  - 600프레임(10초) 동안 새로 만든 오디오 노드 **0개**(최초 6개뿐, 옛 코드와
    같은 수). 매 프레임 하는 일은 `setTargetAtTime` 세 번이다.
  - 실제 서버로 판을 띄워 출격했다 — `engine()` 이 받은 `sp` 가 697.5~1450
    (배율 1.104~1.300), **숫자 아닌 값 0건**, 비행 중 콘솔 오류 **0건**,
    ⏻ 로 홈에 나가면 엔진·바람·지면경고 노드가 전부 정리된다.

  **정정:** 6순위에 근거로 '도플러'가 적혀 있었는데 그건 틀렸다. 엔진은 나와
  같이 움직이므로 내 귀에 도플러는 0 이다. 남는 것은 램 압력뿐이고, 배율을
  1.30 으로 얕게 잡은 것도 그래서다.

  **알고 넘어간 것 — 세기가 완전히 고정되지는 않는다.** 위 표의 RMS 가
  1450 에서 ×1.124 다. 게인(`lvl`)은 한 글자도 안 건드렸는데, `bp` 가 Q 고정
  (1.2)이라 중심을 올리면 대역폭이 같이 넓어져 통과하는 잡음 에너지가 는다.
  일부러 두었다 — 빠른 엔진이 조금 더 큰 것은 맞는 방향이고, 바람이 속도로
  움직이는 폭(사실상 0 → 최대)에 비하면 12%는 훨씬 작아 '세기는 바람'이라는
  분담은 그대로다. 되돌리고 싶으면 게인을 `sqrt(ram)` 으로 나누면 된다.

  **다음 사람에게:** 이 함수를 다시 만지면 하네스에 **속도 300** 경우를 반드시
  넣어라. 거기서 옛 소리와 어긋나면 순항음이 통째로 바뀐 것이다.

- **입력 처리기가 바로 윗줄의 약속을 어기고 있었다** (2026-08-15, QA실) —
  `app.py:268` 은 `# 조종 입력 — 망가진 값이 와도 연결을 끊지 않는다` 라고
  적어 두고 시작한다. **그 아래에서 두 가지 입력이 실제로 접속을 끊었다.**

  원인은 `json.loads` 다. 표준 JSON 에는 없지만 파이썬은 `NaN`·`Infinity`
  리터럴을 받아 준다. 그 값이 산술에 그대로 들어갔다.

  | 보낸 것 | 고치기 전 | 고친 뒤 |
  |---|---|---|
  | `{"aim":[1e308,1e308,1e308]}` | `**2` 에서 OverflowError → **접속 끊김** | `(0.577,0.577,0.577)` |
  | `{"q":Infinity}` | `int()` 에서 OverflowError → **접속 끊김** | 그 패킷만 버림 |
  | `{"aim":[Infinity,0,0]}` | `p.aim = (nan,0,0)` 으로 눌러앉음 | `None`(기수 방향) |
  | `{"pi":NaN}` | **조종간 완전히 당김**(아래) | 0.0 |

  `NaN` 피치가 최대 입력이 된 것은 `min/max` 때문이다. NaN 은 비교가 전부
  False 라 `min(1.0, nan)` 이 `1.0` 을 돌려준다 — 걸러지는 대신 한쪽 끝에
  조용히 붙었다. `_axis()` 를 새로 두어 유한하지 않으면 기본값으로 돌린다.

  크기는 **최대 성분으로 먼저 나눈 뒤** 잰다(`mx * sqrt(Σ(v/mx)²)`).
  제곱이 넘칠 자리가 없어지므로 `try` 로 감쌀 필요가 없다.

  검증: `app.py` 의 `Room.handle` 을 **실물 그대로 불러** 15가지 입력을
  먹였다. 고치기 전 **접속 끊김 2종 · 상태 오염 2종**, 고친 뒤 **0종 · 0종**.
  정상 입력 회귀는 축 값 400,000개 **차이 0개**, 조준 벡터 200,000개 최대
  차이 `4.4e-16`(1 ulp — 정규화 순서가 달라진 끝자리이고, 새 쪽이 수치적으로
  더 정확하다). 경계값 8가지(`[0,0,0]`·`[1e-6,0,0]`·`[1e-200,1e-200,0]` 등)는
  전부 이전과 **같은 값**이다. 봇 60초 전투 격추 12 = 사망 12, 비틀기
  0.0000도, 기수각 180도, 발사 2.8발·초(상한 21.0) — 전부 통과.

  **남이 이걸 못 본 이유**는 정상 클라이언트가 절대 이런 값을 안 보내기
  때문이다. 서버 파일 하나로 끝나고 클라이언트·프로토콜은 안 건드렸다.

- **칸 수 검사가 스냅샷의 31% 만 보고 있었다** (2026-08-15, QA실) —
  `ci.yml` 의 '서버가 보내는 칸 수와 클라가 읽는 칸 수가 맞는가'는 2026-08-13
  에 게임이 망가진 자리를 막으려고 만든 검사인데, **배열 셋(`m`·`fl`·`rd`)만**
  보고 있었다. 스냅샷의 칸은 그 셋이 19칸, `p` 행이 10칸, `me` 사전이 32칸이다.
  **61칸 중 42칸(69%)이 검사 밖**이었다.

  더 나쁜 것이 있었다. 표본이 비면 `continue` 로 넘어가는데, `rd`(레이더)는
  **시야 밖 기체가 있어야만 채워진다.** 검사 시나리오는 6기를 한자리에 띄우므로
  `rd` 는 늘 0행이었다 — 즉 **레이더 칸은 한 번도 검사된 적이 없다.** 통과
  로그에 `rd` 줄이 안 찍히는데 아무도 안 봤다.

  | | 넓히기 전 | 넓힌 뒤 |
  |---|---|---|
  | 검사하는 칸 | 19 / 61 | **61 / 61** |
  | 실제로 표본이 잡히는 배열 | `m` · `fl` | `m` · `fl` · **`rd`** |
  | `p` 행 | 안 봄 | 기본 8칸 + 확장 `[8,9]` |
  | `me` 사전 | 안 봄 | 32칸 집합 대조 |

  `rd` 는 시야를 50m 로 좁힌 스냅샷을 따로 떠서 채웠고(3행), 표본을 못 만들면
  **그것 자체를 실패로** 본다 — 다시는 조용히 건너뛰지 않는다.

  `me` 는 배열과 달리 이름표가 붙어 있어 '칸 수'로는 어긋남이 안 잡힌다.
  서버가 칸 하나를 빼도 클라는 `undefined` 를 읽고 화면에 조용히 `NaN` 을
  찍는다(계기판이 실제로 그랬다). 그래서 **키 집합을 통째로** 못 박았다.

  **검사가 실제로 무는지 확인했다.** 저장소를 임시 폴더에 복사해 일부러
  한쪽만 고쳐 놓고 돌렸다 — 7가지 중 **놓친 것 0건**이다.

  | 일부러 망가뜨린 것 | 결과 |
  |---|---|
  | 아무것도 안 고침(기준선) | 통과 |
  | 서버가 `me.agl` 을 뺀다 | 검출 |
  | 서버가 `me` 에 칸을 늘린다 | 검출 |
  | 서버가 미사일 행을 9칸으로 | 검출 |
  | 서버가 `p` 확장 칸을 셋으로 | 검출 |
  | 클라가 `p` 를 7칸으로 뜯는다 | 검출 |
  | 클라가 플레어를 6칸으로 읽는다 | 검출 |

  검증: 넓힌 블록을 `ci.yml` 에서 **직접 뽑아** 돌려 `m` 8칸(105행) ·
  `fl` 7칸(32행) · `rd` 4칸(3행) · `p` 8+[8,9] · `me` 32칸 전부 일치.
  같은 파일의 다른 파이썬 블록 셋(자세 적분식 · 봇 60초 전투 · 발사 속도)도
  함께 돌려 그대로 통과했다 — 봇 전투는 격추 12 / 사망 12 / 생존 6 /
  발사 165 로 **손대기 전과 같다**(서버 코드는 한 줄도 안 건드렸다).
  `py_compile` 4파일 통과, `ci.yml` 에 탭 문자 0건, heredoc 짝 맞음.
  검증 스크립트는 저장소 밖 임시 폴더에 뒀다.

- **남이 적어 둔 숫자를 다시 쟀다** (2026-08-15, QA실) — 고친 것은 없다.
  **여섯 가지 중 다섯은 그대로 맞았고**, 하나는 검사 자체에 구멍이 있었다
  (바로 위 항목). 새로 나온 것은 상승한계 건이다(맨 위).

  | BACKLOG 가 적어 둔 것 | 다시 재 보니 |
  |---|---|
  | 봇 6기 60초: 격추 12 · 사망 12 · 생존 6 · 발사 165 | **그대로** |
  | 같은 전투의 기수각 179.9956도 · 비틀기 0.0000도 | **그대로** (소수점 넷째 자리까지) |
  | 마하 오차 3,200m `+3.7%` · 17,000m `+15.2%` | **그대로** (표준대기 음속 327.8 · 295.1 m/s) |
  | 스냅샷 칸: 미사일 8 · 플레어 7 · 레이더 4 | **그대로** |
  | "클라가 읽는 `me.*`·`srv.*` 키를 서버가 다 보낸다 — 빠진 것 0건" | **맞다** (독립으로 다시 판 결과도 0건) |
  | 24인 만원 방 스냅샷 | 30초 뒤 9,397 B · 25Hz → **접속자당 229 KB/s**, 미사일 상한 186/186 **포화** |

  마지막 줄만 덧붙일 것이 있다. 미사일이 상한에 붙어 있다는 것은 **상한이
  없었으면 더 컸다**는 뜻이다(시야 안 463발 중 186발만 실린다). 즉 24인 방의
  스냅샷 크기는 지금 `maxMissiles` 가 정하고 있고, 그 값을 올리면 대역폭이
  바로 따라 오른다. '정비공조절 40~400' 이라 적혀 있으니 올릴 때 이걸 볼 것.

  키 대조는 클라 JS 를 실제로 훑어서 했다 — `const ctx = …, s = W.srv` 같은
  **여러 선언이 한 줄에 있는 경우**와 `corners(ctx, s)` 처럼 **매개변수로
  넘기는 경우**까지 따라가야 `THR`·`FLR`·`G`·`LOCKED` 를 읽는 곳이 보인다.
  안 따라가면 그 칸들이 '아무도 안 읽는 칸'으로 잘못 잡힌다 — 실제로 처음에
  그렇게 나왔다. **다음에 이 대조를 다시 할 사람은 이 두 가지를 조심할 것.**

- **계기판의 G 경고가 없는 규칙을 지어내고 있었다** (2026-08-15, UI실) —
  5순위 6번의 (a). 지시문이 짚은 '오늘 할 일' 셋은 앞선 세 커밋에서 이미
  끝나 있어(AOA·STALL·죽은 `pitchLadder` / `RKT 99`·`MSL 99` / 리시 눈금),
  BACKLOG 에서 **UI실 담당 파일 안에서 혼자 끝낼 수 있는** 다음 항목을 골랐다.

  | 걷어낸 곳 | 이전 | 이후 |
  |---|---|---|
  | `hud.js` `corners()` | `Math.abs(s.g) > 7` 이면 G 칸 호박색 | 항상 초록 |
  | `hud.js` `warnings()` | `Math.abs(s.g) > 8.5` 이면 `OVER G` | 없음 |

  **문턱이 기체 제원과 무관했다.** 서버는 `gload = 1 + |pr|·4`,
  `pr = 피치명령 × pitchRate`(game.py:370·406)로 만들고, 기수 추종 제어기는
  `c_pitch = dP / pitchRate × 9.0`(input.js `KP`)로 명령을 만든다. 곱하면
  pitchRate 가 약분되어 **`gload = 1 + 36·dP`** 만 남는다. 되풀면:

  | 문턱 | 피치 조준 오차 | 기체별 차이 |
  |---|---|---|
  | `G > 7` (호박색) | **9.55도** | 없음 (세 기종 동일) |
  | `G > 8.5` (`OVER G`) | **11.94도** | 없음 (세 기종 동일) |

  기본 리시가 28도인데 **그 3분의 1도 안 벌어져서** 경고가 떴다. 지면 회피
  보조는 피치를 1.0 까지 밀므로(`input.js`) 작동할 때마다 떴다.

  **실측(봇 6기 60초, 16,425 기체·틱).** `random.seed(20260810)` — CI 의
  테스트 파일럿과 같은 시나리오다.

  | | 화면에 떠 있던 비율 |
  |---|---|
  | G 칸 호박색 (`>7`) | **23.5%** |
  | `OVER G` (`>8.5`) | **18.4%** |
  | 최고 gload | 20.2 (flanker 상한과 일치) |

  즉 평범한 전투의 **다섯 틱 중 하나**에서 `OVER G` 가 떠 있었고, 넘겨도
  아무 일이 없었다. 경고가 늘 떠 있으면 경고가 아니다 — `RKT 99` 를 걷어낸
  것과 같은 종류다.

  **넘겨도 아무 일이 없다는 근거.** `gLimit`(falcon·eagle 30 · flanker 28)을
  강제하는 곳이 서버에 0건이고(`game.py:398` 이 그렇게 적어 두었다), 애초에
  식의 최대값(18.2 · 15.2 · 20.2)이 **자기 gLimit 에 닿지도 못한다.**

  **숫자는 남겼다.** 조종간을 얼마나 당기고 있는지는 읽을 값이다. 스냅샷
  `me.g` 칸도 그대로라 CI 의 칸 수 검사에 영향이 없다.

  **다만 `me.g` 의 독자가 이제 `hud.js:649` 한 줄뿐이다.** 익단 와류가 예전
  `srv.g > 6` 에서 자세 차분(`pl.rate[1]`)으로 옮겨 갔기 때문이다 —
  `scene.js:1705` 에 남은 것은 그 사실을 적은 **주석**이고 실제 독자가
  아니다(처음에 이걸 독자로 잘못 읽고 주석에 그렇게 적었다가 고쳤다).
  G 칸을 언젠가 마저 빼면 `me.g` 는 4순위 `st` 와 같은 '아무도 안 읽는 칸'이
  되므로 그때는 서버와 같은 커밋이어야 한다. `hud.js` 주석에도 적어 두었다.

  **남은 것은 5순위 6번에 (c) 로 적어 두었다** — 선택 화면의 "30G"(`main.js`)와
  이제 사실이 아닌 `game.py:398~402` 주석. 둘 다 오늘 배정된 세 파일 밖이다.

- **설정이 표현할 수 없는 기본값을 띄우고 있었다** (2026-08-15, UI실) —
  지시문 '오늘 할 일' 3번(도움말이 실제 값과 맞는가). 옛 9순위를 끝냈다.
  1·2번(AOA·STALL·죽은 함수 / `RKT 99`·`MSL 99`)은 앞선 두 커밋에서 이미
  끝나 있어 오늘 남은 것은 3번뿐이었다.

  리시 기본값은 **28도**인데(`main.js:70`, `leashv` 3판) 그 28 이 세 곳에
  반영돼 있지 않았다.

  | 고친 곳 | 이전 | 이후 |
  |---|---|---|
  | `index.html:101` | `step="5"` — 눈금이 25·30·35…90 | `step="1"` |
  | `index.html:84` | 주황 X 는 '70도 이상' 에서 나온다 | '66도보다 크게' + 기본값 28도 명시 |
  | `input.js:258` | 폴백 `this.set?.leash ?? 55` | `?? 28` |

  **슬라이더가 기본값을 표현하지 못했다.** `range` 는 눈금에서 벗어난 value 를
  가까운 눈금으로 스냅한다. `main.js:436` 이 `e.value = 28` 을 넣으면 브라우저는
  **30** 으로 반올림하는데, 라벨은 `settings.leash` 에서 따로 찍으므로 `28°`
  라고 적힌다. 설정을 열면 **손잡이 30 · 글자 28°** 였고, 한 번 건드리면 5의
  배수로만 움직여 28 로 돌아갈 방법이 없었다.

  '70도 이상' 은 눈금이 5단위였을 때만 맞는 말이었다(다음 눈금이 70). 눈금이
  1도가 됐으니 실제 경계는 서버 `max_off=1.15rad` = **65.8901도**, 즉 66부터
  넘어간다. 다만 66에서는 여유가 0.11도뿐이라 '66도 이상' 이 아니라
  **'66도보다 크게'** 로 적었다.

  `input.js` 의 `?? 55` 는 두 판 전 기본값이다. **이 폴백은 지금 실제로
  안 쓰인다** — `main.js:95` 가 언제나 `settings` 를 넘긴다. 런타임 동작이
  바뀐 것은 없고, 기본값을 옮길 때 같이 안 옮긴 자리를 맞춘 것이다.
  같은 부류가 서버에도 하나 남아 있다(9순위 '덤' 참조).

  검증: **크롬으로 고치기 전/후를 A/B 했다.** 윈도우 크롬은 콘솔에서 stdout 이
  끊겨 `--dump-dom` 이 빈 값을 준다 — 디버깅 포트(CDP)로 붙어 `Runtime.evaluate`
  로 값을 직접 읽었다. 정적 서버 두 대(8141 = 이후 · 8142 = `step` 만 5로
  되돌린 이전)에 같은 프로브를 물렸다.

  | | 고치기 전 `step=5` | 고친 뒤 `step=1` |
  |---|---|---|
  | `settings.leash` | 28 | 28 |
  | 슬라이더가 남긴 값 | **30** | **28** |
  | 라벨이 찍는 글자 | `28°` | `28°` |
  | 둘이 일치하나 | **아니다** | **그렇다** |

  그 뒤 **실제 게임을 통째로 띄워** 확인했다(`server/app.py --port 8151` +
  `main.js` 그대로 부팅): 슬라이더 28 · 라벨 28° 로 일치하고 **콘솔 오류
  0건**이다. `node --check` 는 `hud.js`·`input.js`·`main.js` 통과.
  검증 스크립트(정적 서버 + CDP 프로브)는 저장소 밖에 뒀다.

  **다음 사람에게 남기는 함정** — 이 페이지는 rAF 루프가 계속 돌아
  `--virtual-time-budget` 이 만료돼도 `--dump-dom` 이 안 끝난다. 헤드리스로
  값을 읽을 거면 처음부터 CDP 로 가라. (앞 항목이 적어 둔 서비스워커
  `location.reload()` 함정과는 별개다.)

- **땅에 다가가는데 아무 소리도 안 났다** (2026-08-15, 사운드실) — 6순위 4번의
  남은 절반. 미사일 경고(RWR)는 진작 울고 있었고 비어 있던 것은 지면 접근
  경고뿐이었다. `audio.js` 에 `ground()`/`stopGround()` 를 넣고 `main.js` 가
  살아 있는 동안 매 프레임 서버 `agl` 을 넘긴다. **실속 경고는 만들지 않았다** —
  지시문이 못 박은 대로 서버가 `stalling` 을 영영 False 로 둔다(4순위).

  **높이가 아니라 다가오는 속도로 운다.** 그래서 `vy` 를 안 쓰고 `agl` 의
  변화율을 잰다 — 수평으로 날아도 앞의 산이 솟으면 `vy` 는 0 인데 `agl` 은
  줄어든다. 대신 낮게 스치기만 하는 비행(2순위가 권하는 저공)은 접근 속도가
  0 이라 **한 번도 안 운다.** 눈금은 전부 서버 값에서 끌어왔다.

  | 무엇 | 값 | 근거 |
  |---|---|---|
  | 바닥 | `agl` 8 | 서버가 지면+8m 에서 격추로 친다(`game.py:568`) |
  | 켜지는 지점 | 충돌 6초 전 | 접근 3m/s 아래는 무시(살살 내려앉는 것까지 잡으면 저공 내내 운다) |
  | 삐 간격 | 620 → 120ms | 충돌이 가까울수록 좁아진다 |
  | 음높이 | 525~960Hz 두 음 교대 | 미사일 경고(420Hz 한 음)와 귀로 갈린다 |
  | 크기 | 충돌 3초 전에 미사일 경고와 같다 | 아래 표 |

  검증은 **헤드리스 크롬에서 실제 Web Audio 로 렌더링해 파형을 쟀다**
  (마스터 1.0 기준). `OfflineAudioContext` 에 `currentTime` 만 가짜로 흘려
  넣어 60fps 프레임 호출을 그대로 재현했다.

  | 상황 | 삐 | 최대진폭 | 간격 |
  |---|---|---|---|
  | 순항 3000m 수평 | **0회** | — | — |
  | 저공 60m 수평(스치기) | **0회** | — | — |
  | 2000m 에서 초속 600m 강하 | 17회 | 0.138 | 0.60→0.13s |
  | 수평인데 산이 400m→0 으로 솟음 | 22회 | 0.139 | 0.60→0.13s |
  | 강하하다 3초에 기수를 듦 | 11회 뒤 **멎음** | 0.126 | — |

  같은 하네스로 잰 다른 소리: 미사일 경고 0.093 · 락온 0.089 · 로켓 0.088 ·
  폭발 0.12~0.19 · 엔진(AB) RMS 0.022 · 바람(AB·지면) RMS 0.025.
  경보 구간 RMS 는 0.019 다 — **엔진·바람과 비슷한 크기에 음색만 다르다.**

  **실제 게임으로도 확인했다.** 서버(포트 8098)를 띄우고 CDP 로 붙어 판에
  출격한 뒤, 서버가 주는 `agl` 만 가로채 강하를 흉내냈다. 수평 3000m 에서
  0회, 초속 400m 강하에서 18회, 다시 상승하면 멎었다. 엔진음·바람·지면경고
  세 노드가 모두 살아 있고 `ctx` 는 `running`. **콘솔에 새 오류 0건** —
  남는 것은 고치기 전에도 있던 둘뿐이다(`index.html` 의 deprecated meta,
  헤드리스에서만 나는 포인터 잠금). `node --check` 는 두 파일 다 통과.

  **매 프레임 노드를 새로 만들지 않는다**(지시문). 오실레이터와 게인을 한 번만
  만들고 그 뒤로는 봉투와 주파수만 옮긴다 — 600프레임 동안 새로 만든 노드가
  **0개**임을 셌다. `agl` 이 `undefined`·`NaN`·`null`·음수·`1e9` 로 와도
  600프레임에서 예외 0건, 게인은 0.0001~0.14 안에 머문다.

  **다만 "좋게 들린다"는 확인이 아니다.** 나는 소리를 못 듣는다. 위는 전부
  숫자다. 특히 이 둘은 사장님이 직접 날아 보고 정해 주셔야 한다.
  - 저공 전투에서 **거슬리게 자주 우는지**. 지금은 '가라앉는 중'일 때만 울지만
    근접전에서 기수를 내리는 일은 흔하다. 시끄러우면 켜지는 지점을 6초에서
    4초로 줄이면 된다(`ground()` 의 `ttc / 6`).
  - 엔진·바람에 **묻히지 않는지**. 위 RMS 는 비슷한데 이쪽은 순음이라
    보통은 뚫고 나온다. 안 들리면 `lvl` 의 0.095 를 올린다.

  ### 뒤이어 잡은 것 — 경보가 **켜지는 순간**에 드르륵 울었다

  위 검증에는 구멍이 있었다. 잰 강하가 전부 **이미 경보 범위 안에서 시작하는
  것**뿐이다(초속 600m 로 2000m 강하 → 문턱이 3600m 라 첫 프레임부터 켜져
  있다. 산이 400m→0 으로 솟는 것도 마찬가지다). 그래서 **문턱을 천천히 걸어
  들어가는 경우를 한 번도 안 지나갔는데, 실제 비행은 거의 전부 그쪽이다.**

  스냅샷은 20Hz 인데 이 함수는 화면 60Hz 로 불린다. 그래서 접근 속도가 ±5%
  로 흔들린다(실측 188.7 · 200.4 · 210.9 m/s). 경보가 막 켜질 때는 `ttc` 가
  문턱 6초 언저리를 오르내리는데, **조용한 프레임마다 `p.next = 0` 으로
  재장전을 지워 버려서** 다음 프레임에 곧바로 삐가 나갔다.

  | 2000m 에서 초속 200m 강하 | 고치기 전 | 고친 뒤 |
  |---|---|---|
  | 첫 여덟 번의 간격 | **67~100ms** | 650 · 633 · 550 · 533 · 450 · 400 · 368ms |
  | 가장 좁은 간격 | 67ms | **132ms**(설계 눈금) |
  | 간격이 단조롭게 좁아지는가 | **아니다** | 그렇다 |
  | 문턱을 오르내리며 재장전이 지워진 횟수 | 8회 | 0회 |

  즉 **가장 안 급한 순간에 제일 빠르게 울고, 정작 가까워지면 633ms 로
  느려지는** 정반대의 소리였다. `p.next` 를 그냥 두는 것으로 고쳤다(한 줄).
  한동안 안 울리다 다시 켜지는 경우는 `p.next` 가 이미 과거라 그대로 즉시 운다.

  이 판의 숫자는 진짜 Web Audio 로 렌더링해 다시 쟀다(마스터 1.0 기준):
  삐 27회, 간격 652 → 132ms 단조 감소, 진폭 0.044 → 0.135. **충돌 3초 전
  진폭이 0.090 으로 미사일 경고(0.093)의 97%** 이고, 최대값도 폭발음
  (0.181)의 74% 다. 실제 서버로 판을 띄워 출격한 확인도 다시 했다 — 순항
  중 서버 `agl` 이 그대로 들어오고(2891.6 · 2993.7 · 3001, 숫자 아닌 값 0건)
  삐 0회, 강하로 바꾸면 울고, 기수를 들면 멎고, ⏻ 로 홈에 나가면 노드가
  정리된다. **콘솔 오류는 고치기 전(2a3dd28)과 같은 하네스로 A/B 해서
  양쪽 다 1건**이다(헤드리스 전용 포인터 잠금) — 새 오류 0건.

  **다음 사람에게:** 이 함수를 다시 만지면 하네스에 **문턱을 천천히 걸어
  들어가는 강하**(예: 2000m 에서 초속 200m)를 반드시 넣어라. 급강하만 재면
  이 부류의 버그는 화면에도 숫자에도 안 나온다.

- **화면 세 곳이 없는 규칙(무장 선택)을 말하고 있었다** (2026-08-15, UI실) —
  지시문 '오늘 할 일' 3번(도움말이 코드와 맞는가). 옛 8순위를 (a) 로 끝냈다.

  무엇이 나갈지 정하는 것은 **누른 버튼**뿐이다(`game.py:509·511` 이
  `want_fire → ROCKET` · `want_missile → MISSILE` 로 가르고 `_launch()` 는
  `p.weapon` 을 안 본다). 그런데 1·2 키가 `input.js` 의 `weapon` 을 바꾸고,
  그 값이 서버까지 갔다가 `s.w` 로 돌아와 계기판에 `[ ROCKET ]`/`[ AIM-9 ]`
  로 찍혔다. **키를 누르면 글자만 바뀌고 발사는 하나도 안 달라졌다.**

  | 고친 곳 | 이전 | 이후 |
  |---|---|---|
  | `input.js` | `Digit1`·`Digit2` 가 `weapon` 을 0/1 로 바꿈 | 키 삭제, `weapon` 은 상수 0 |
  | `input.js` | 부르는 곳 0건인 `onWeapon` 콜백 | 삭제 |
  | `hud.js:679` | `s.w` 로 `[ ROCKET ]`/`[ AIM-9 ]` | `좌클릭 RKT · 우클릭 MSL` |
  | `index.html:77·170` | `1` `2` 무장 전환 | 걷어냄 |

  **덤으로 조작키를 전부 코드와 대조했다.** 도움말은 그냥 'Ctrl' 이라고
  적어 두는데 `input.js` 는 `ControlLeft` 만 봤다 — **오른쪽 Ctrl 로만
  조작하면 스로틀이 안 내려갔다.** Shift 는 진작 양쪽을 보고 있었으므로
  그쪽에 맞췄다(`ControlRight` 추가). 나머지 키(W·X·Space·F·C·A·D·T·V·H)는
  전부 코드와 맞았다.

  **원뿔 각도는 맞았다.** 도움말의 66도는 서버 `aim_dir` 의
  `max_off=1.15rad`(=65.9도)와 같고 `hud.js:252` 도 같은 값을 쓴다. 다만
  그 값은 **기수 기준 반각**이라 문구를 '기수에서 66도'로 명확히 했고,
  리시 기본값이 28도라 **조준점 여유를 70도 이상으로 올려야만 주황 X 를
  볼 수 있다**는 사실을 덧붙였다(예전 문구는 늘 뜨는 것처럼 읽혔다).
  — *뒤에 정정: '70도' 는 슬라이더 눈금이 5단위였을 때의 다음 눈금이었다.
  눈금을 1도로 고치면서 실제 경계인 '66도보다 크게' 로 다시 적었다.
  위 '설정이 표현할 수 없는 기본값' 항목 참조.*

  검증: **고치기 전을 같은 하네스로 A/B 했다.** 작업 트리에서 내 세 파일만
  HEAD 로 되돌린 사본을 따로 띄워(다른 실의 커밋 안 된 `jet.js` 는 양쪽에
  똑같이 둬서 변수에서 뺐다), 실제 서버 두 대(8131 = 이후 · 8132 = 이전)에
  헤드리스 크롬으로 붙어 판을 시작하고 계기판이 `fillText` 로 찍는 글자를
  전부 가로챘다.

  | | 고치기 전 | 고친 뒤 |
  |---|---|---|
  | 무장 칸 | `[ ROCKET ]` | `좌클릭 RKT · 우클릭 MSL` |
  | 1·2 를 눌렀을 때 나간 `w` | **0 과 1** | **0 뿐** |
  | `w` 칸이 빠진 입력 패킷 | 0건 | **0건**(형식 유지) |
  | 콘솔 오류 | 포인터 잠금 1건 | **같다** |

  `SPD`·`ALT`·`G`·`THR`·`RKT ∞`·`MSL ∞`·`FLR 20`·레이더 거리·락온 패널·
  `여기를 겨냥` 은 전후 모두 그대로 찍힌다. 남는 오류 1건은 고치기 전에도
  나던 헤드리스 전용 포인터 잠금이다.

  키 처리는 **브라우저 없이 따로 쟀다** — 헤드리스 swiftshader 는 초당 0.5
  프레임이라(전체 실행에서 `fillText` 102회) 키를 누른 효과를 프레임으로
  분간할 수 없었다. 최소 DOM 스텁 위에 실제 `input.js` 를 그대로 import 해
  19가지를 확인했고, **고치기 전 판으로 같은 시험을 돌려 14/19** 가 나온다.
  나머지 5개가 정확히 이번에 고친 것들이다(1·2 키 3개 · `ControlRight` 1개 ·
  좌우 Ctrl 대칭 1개). **회귀는 0건이다** — 통과하던 14개는 그대로 통과한다.

  `node --check` 는 `hud.js`·`input.js` 통과. 도움말·홈 화면을 실제로
  렌더링해 `무장 전환`·`1 2 무장`·`무장 선택` 이 **0건**이고 `1`·`2` `kbd`
  요소가 **0개**임을 확인했다. 검증 스크립트는 저장소 밖에 뒀다.

  **다음 사람에게 남기는 함정 하나** — 이 게임의 서비스워커는 갱신을 잡으면
  `location.reload()` 를 한다(`main.js:190`). 헤드리스로 A/B 할 때 이걸 안
  끄면 페이지가 한 번 새로 뜨면서 WebSocket 이 끊기고, 후킹해 둔 `fillText`
  가 통째로 날아가 **계기판이 안 찍히는 것처럼 보인다.** 실제로 여기서 한 번
  속았다. `navigator.serviceWorker.register` 를 문서 로드 전에 막아야 한다.

- **기체 표면 마이크로 범프를 넣었다** (2026-08-14, 아트실) — 지시문 '오늘 할 일'
  2번. 알베도와 거칠기는 이미 얼룩져 있었지만 **법선이 면마다 한 값**이라
  하이라이트는 여전히 기체를 통짜로 미끄러졌다. 오브젝트 공간에서 절차적 높이의
  기울기를 재 법선에 얹는다(surface gradient). UV 도 이미지도 안 쓴다.

  | 잰 것 | 값 |
  |---|---|
  | 범프 파장 대역 대비(2차차분 RMS) | 1.824 → 3.307 (**1.81배**) |
  | 평균 밝기 | -2.65% (기운 면이 거울 방향에서 벗어난 몫) |
  | 전면 100% 커버리지 비용 | 4.063 → 4.577ms (**+0.515ms**) |
  | 도장면 최대 화면 점유(5m·화각 75) | **14.07%** → 실전 **+0.072ms** |
  | varying | 19 → 22 float (6 → **7행**, ES 1.00 보장치 8행) |

  지시문 예산 +0.15ms 안이다. 커버리지는 실제 `buildJet()` 메시(3776 삼각형)를
  소프트웨어 래스터라이즈해 거리별로 쟀고, 5m 는 충돌 거리다(12m 6.59% ·
  20m 4.28%). 비용·대비는 1920×1080 · Intel Iris Xe / ANGLE D3D11 에서 HEAD 판과
  작업본을 **같은 페이지·같은 GPU 상태**로 A/B 했다. pbr 12변형(기본/SKIN/
  SKIN+DIRECT_OUT × highp/mediump) 컴파일·링크 통과, GL 오류 0건.

  **다음 사람에게 남기는 두 가지.**
  - varying 이 7행이다. **vec3 를 하나만 더 늘려도 8행이 차서 ES 1.00 보장치에
    닿는다.** 그 다음은 varying 을 합치는 일이 먼저다.
  - 값노이즈의 기울기는 해석적으로 나온다(`vnoised`). 처음에는 기존 `nGrad`
    (vnoise 3회)를 썼는데 그것만으로 +0.83ms 였다. 해시 4번짜리로 바꿔 +0.515ms
    가 됐다. **노이즈 기울기가 필요하면 차분을 쓰지 마라.**

- **로켓과 유도탄이 같은 소리였다** (2026-08-14, 사운드실) — 6순위 3번.
  서버는 진작 무기 종류를 보내고 있었다(`game.py` `_launch` → `{"e":"launch",
  "id":…, "k":kind}`, `ROCKET=0` · `MISSILE=1`). `main.js` 의 `onEvent` 가
  **그 칸을 버리고** 두 무기 모두 `sfx.missile()` 하나로 울렸다. 좌클릭과
  우클릭이 소리로 구분되지 않았다.

  고친 것은 세 줄이다. `main.js` 는 `ev.k === 0` 이면 `rocket()`, 아니면
  `missile()` 을 부른다(모르는 값이면 유도탄 — 여태 나던 소리를 유지한다).
  `audio.js` 의 죽어 있던 `fire()`(부르는 곳 0건)를 `rocket()` 으로 바꿔
  다시 쓰고, `missile()` 은 성격을 뒤집었다.

  **성격은 서버 값에서 끌어왔다.** 로켓은 재장전 0.42초에 초속 900,
  유도탄은 0.9초에 초속 400으로 4초를 태운다(`WEAPONS`).

  | | 로켓(무유도) | 유도탄(IR) |
  |---|---|---|
  | 어택 | 3ms — 앞이 선다 | 40ms(점화) · 55ms(모터) — 밀려 나온다 |
  | 필터 | 하이패스 1100Hz | 로우패스 1100→260Hz |
  | 길이 | 짧게 | 길게 |

  예전 `missile()` 은 220→900Hz 로 **올라가는** 톱니 하나였다. 밝고 위로
  솟아 로켓보다도 날카로웠다 — 방향을 반대로 잡았다.

  검증: 헤드리스 크롬에서 `OfflineAudioContext` 로 **실제로 렌더링해 파형을
  쟀다.** (마스터 1.0 기준, 밝기는 영교차율)

  | 소리 | 최대진폭 | 어택 | 들리는 길이 | 밝기 |
  |---|---|---|---|---|
  | 로켓 | 0.092 | 4.5ms | 52ms | 9559Hz |
  | 유도탄 | 0.064 | 29ms | 269ms | 603Hz |
  | (폭발음) | 0.140 | 19ms | 203ms | 974Hz |

  처음 잡은 로켓 게인은 최대진폭 0.142 로 **폭발음과 거의 같았다.** 0.42초
  마다 울리는 소리가 폭발만큼 크면 피곤하다 — 재서 알고 30% 내렸다.
  날카로움은 크기가 아니라 어택과 고역에서 나온다.

  게임 페이지를 실제로 띄워 **콘솔 오류 0건**, `node --check` 통과, 첫 입력
  전(`ctx` 없음)·소리끔·40연발 throttle 경로 모두 예외 0건을 확인했다.

  **다만 "좋게 들린다"는 확인이 아니다.** 나는 소리를 들을 수 없어서 위
  숫자로 대신했다. 실제로 쏴 보고 판단해 주셔야 한다. 특히 로켓이 연사할 때
  거슬리는지, 유도탄이 너무 묻히지는 않는지.

- **아무도 안 읽는 스냅샷 네 칸을 걷어냈다** (2026-08-14, 프로그래밍실) —
  `aoa` · `bo` · `am` · `ms`. 앞 둘은 옛 항공역학 분기가 사라진 뒤로 서버가
  `0.0` 에서 한 번도 안 바꾸는 값이었고, 뒤 둘은 탄약이 어디서도 줄지 않아
  영원히 `99` 였다. **매 틱 25번, 사람 수만큼 그냥 버려지고 있었다.**

  UI실이 계기판에서 AOA · STALL · `RKT 99` · `MSL 99` 를 걷어낸 뒤로 이 네
  칸은 클라이언트 읽는 곳이 **0건**이 됐다. 그래서 서버에서 뺐다.

  같이 지운 것 — 값을 만들던 쪽이 통째로 죽어서:
  · `Plane.aoa` · `Plane.blackout` (초기화·재출격에서만 쓰이던 상수)
  · `Plane.ammo` · `Plane.missiles` 와 이들이 읽던 `WEAPONS[*]["ammo"]`
  · `world.js` 의 `_prevMs` — `me.ms` 를 받아 두기만 하던 줄이다. 그 줄
    주석이 "다른 곳에서 쓸 수 있으니 추적만 남긴다"고 적어 뒀는데, 쓰는
    곳은 끝내 안 생겼다. 서버와 **같은 커밋에서** 지웠다.

  `st`(실속)는 **일부러 남겼다.** 같은 부류의 죽은 값이지만 `scene.js` ·
  `main.js` 가 아직 읽어서 프로그래밍실 혼자 못 뺀다 — 4순위로 남겼다.

  | | 걷기 전 | 걷은 뒤 |
  |---|---|---|
  | `me` 칸 수 | 36 | **32** |
  | `me` 직렬화 | 362 B | **327 B** (−35 B, −9.7%) |
  | 사람당 | — | **−875 B/s** |
  | 24인 만원 방 | — | **−20.5 KB/s** |

  검증: `py_compile` 통과 · `node --check` 통과 · 봇 6기 60초 전투에서
  격추 12 = 사망 12, 자체 기울기 0.0도, 기수각 최대 180도, 생존 6대,
  발사 165회(2.8/초). 스냅샷 칸 수 대조는 미사일 8/8 · 플레어 7/7 ·
  레이더 4/4 일치. 더해서 **클라이언트 전체에서 `me.*` · `srv.*` 로 읽는
  모든 키를 서버가 실제로 보내는 키와 대조**했다 — 빠진 것 0건.

- **계기판의 `RKT 99` · `MSL 99` 를 걷어냈다** (2026-08-14, UI실) — 로켓과
  유도탄은 탄약이 무제한이다. 서버는 99 로 세팅만 하고 어디서도 줄이지
  않으며(game.py:606) 발사를 막는 것은 쿨다운뿐이다(game.py:508 의 주석이
  "둘 다 탄약 무제한이다"라고 못 박아 두었다). 그 99 를 띄우고 있으면
  **'아껴 쓰자'는, 게임에 없는 규칙을 화면이 지어내는 것**이다. 숫자를 빼고
  `RKT ∞` · `MSL ∞` 만 남겼다.

  **플레어는 안 건드렸다.** `s.fla` 는 진짜로 줄어드는 유일한 잔량이다
  (game.py:627 에서 감소, CFG 20발). 숫자와 0 일 때 붉게 뜨는 경고를 그대로
  뒀다 — 세 칸이 같아 보이지만 셋 중 하나만 진짜다.

  재장전 시간으로 바꾸는 쪽은 **지금은 못 한다.** 서버가 `fire_t`·`ms_cd`
  를 스냅샷에 안 싣는다(4순위로 넘겼다). 클라가 제 발사 시각을 스스로
  세어 흉내 낼 수는 있지만, 발사 여부는 서버가 정하므로 그건 추측이다.

  검증: 실제 서버(포트 8099)로 헤드리스 크롬에서 판을 띄우고 12초 동안
  **계기판이 `fillText` 로 찍는 글자를 전부 가로채** 고치기 전후를 같은
  하네스로 비교했다.
  | | 고치기 전 | 고친 뒤 |
  |---|---|---|
  | 무장 칸 | `RKT 99` · `MSL 99` · `FLR 20` | `RKT ∞` · `MSL ∞` · `FLR 20` |
  | 화면에 뜬 `99` | 있다 | **0건** |
  | 콘솔 오류 | 포인터 잠금 1건 + deprecated meta 1건 | **같다** |

  SPD·ALT·방위·G·THR·FLR·레이더·락온 패널·MISSILE 경고·BUILD 도장은 전후
  모두 그대로 찍힌다. 남는 오류 둘은 **고치기 전에도 나던 것**이다 —
  포인터 잠금은 헤드리스에서만 나고, deprecated meta 는 `index.html` 의
  PWA 태그다(사운드실도 같은 2건을 기록했다). 하네스는 실행마다 서비스워커와
  HTTP 캐시를 껐다 — 안 그러면 두 번째 실행이 첫 번째의 JS 를 받아 비교가
  통째로 무의미해진다. `node --check` 는 hud.js·input.js·main.js·sw.js 통과.
  검증 스크립트는 저장소 밖 임시 폴더에 뒀다(배포에 섞이지 않게).

  **다음 사람을 위한 함정 하나** — 헤드리스 크롬은 한동안 띄워 두면 창을
  가려진 것으로 보고 rAF 를 멈춘다. 그러면 게임은 계속 붙어 있는데(body 에
  `flying` 이 그대로 있다) **계기판이 한 글자도 안 찍혀** 마치 HUD 가
  깨진 것처럼 보인다. 실제로 이 검증에서 한 번 속았다. 크롬을
  `--disable-renderer-backgrounding --disable-backgrounding-occluded-windows`
  `--disable-background-timer-throttling --disable-features=CalculateNativeWinOcclusion`
  로 띄우면 사라진다. 3D 는 `--enable-unsafe-swiftshader --use-angle=swiftshader`
  가 있어야 뜬다.

- **코드가 하는 일과 다른 말을 하던 주석을 고쳤다** (2026-08-14, 프로그래밍실)
  — **코드는 한 줄도 안 바꿨다.** 지시문 2번(주석과 코드가 다른 곳)이다.

  | 고친 곳 | 뭐라고 적혀 있었나 | 실제 |
  |---|---|---|
  | `game.py` 문서주석 | "받음각 양력계수 · 유도항력 k·CL² · 천음속 항력 · 동압 비례 조종면 · 구조 G 한계" | 그 분기는 2026-08-14 에 지웠다. 지금은 **아케이드**다 |
  | `game.py:938` 봇 로켓 리드 | 추력/질량 = **15000** m/s² | **1000** (60000/60). 15배 틀렸다 |
  | `RESUME.md:138` | 같은 15000 | 같이 고쳤다 |

  문서주석이 제일 나빴다. **파일을 여는 사람이 맨 처음 읽는 다섯 줄**이
  없는 공력 모델을 설명하고 있었다. 실제 코드는 90줄 아래에서 "실속·G 한계·
  에너지 손실 없이"라고 정반대를 말한다. 15000 쪽은 동작에는 영향이 없다 —
  계산은 `WEAPONS[ROCKET]` 에서 직접 뽑으므로 **숫자가 주석에만 있었다.**

  덤으로 확인한 것(주석에 근거로 적어 뒀다): 로켓은 연소 1.2초 동안 정확히
  **1800m** 를 날아가고 봇은 1800m 안에서만 쏘므로, 리드 계산의 등가속
  가정이 사거리 전체를 덮는다. 1800m 에서의 비행시간이 1.200초로 연소시간과
  정확히 같다.

  검증: 봇 6기 60초 전투가 **고치기 전과 비트 단위로 같다** — 격추 12 /
  사망 12 / 생존 6 / 발사 165 / 기수각 179.9956도 / 비틀기 0.0000도,
  여섯 기의 위치·자세·HP·전적 해시까지 동일
  (`e37970026d421c81e3ab7ddb3fbcd93d`). HEAD 를 임시 디렉터리에 풀어
  **같은 스크립트로 양쪽을 돌려** 비교한 값이다. 그 밖에 `py_compile` 3파일,
  `node --check` 클라 JS 16파일, CI 의 정합성 검사 3종(회전율이 서버 한
  곳뿐인가 · 자세 적분식이 서버=클라인가 · 삭제된 이름 참조 0건) 통과,
  스냅샷 칸 수는 미사일 8 · 플레어 7 · 레이더 4 가 world.js 와 일치
  (셋 다 빈 배열이 아닌 상태로 쟀다 — 플레어는 `want_flare` 를 켜고,
  레이더는 시야를 50m 로 좁혀 강제로 채웠다).

  **world.js · m3d.js 는 안 건드렸으므로 BUILD·`sw.js` 는 올리지 않았다.**

  이때 함께 고친 것: BACKLOG 가 인용하던 `game.py` 줄번호가 문서주석이
  길어지며 전부 +7 밀렸다(600→606, 616→623, 176→183, 1101·1111→1113·1123,
  335→341, 710→716). 같이 맞췄다.

- **바람 소리를 넣었다** (2026-08-14, 사운드실) — 속도가 붙을수록 커지는
  광대역 잡음. `audio.js` 의 `wind()`/`stopWind()`, 엔진음과 **같은 방식**이다
  (노이즈 루프 하나를 만들어 두고 주파수와 게인만 옮긴다).

  **소리는 아무도 안 들어 봤다.** 이 환경에서는 들을 수가 없다. 아래는 전부
  수치와 코드 경로 검증이고, **"좋게 들린다"는 근거가 아직 없다.**

  속도 눈금은 지어내지 않고 서버 `CFG` 를 그대로 따랐다(`arcMin` 300 ·
  `arcMax` 830 · `arcBoost` 1450 m/s). 제동 중(최저 165)에는 안 들리게
  260 을 문턱으로 뒀다.

  | 상황 | 고도 3000m | 지면 | 저역차단 |
  |---|---|---|---|
  | 제동 | 0.0001 (무음) | 0.0001 | 500Hz |
  | 스로틀 0 (300m/s) | 0.0011 | 0.0017 | 587Hz |
  | 스로틀 50% (565) | 0.0118 | 0.0183 | 1166Hz |
  | 스로틀 100% (830) | 0.0249 | 0.0385 | 1745Hz |
  | 애프터버너 (1450) | 0.0601 | 0.0931 | 3100Hz |

  엔진음 대비 스로틀 100% 에서 23%, 애프터버너에서 35% 다 — **바람이 엔진을
  덮으면 스로틀이 안 들리므로** 일부러 아래에 뒀다. 저공(AGL 0)은 600m 위의
  1.55배이고, 600m 는 2순위가 저공으로 잡아 둔 눈금과 같다.

  검증: 가짜 WebAudio 위에서 실제 `wind()` 를 돌려 위 표를 쟀고,
  **600프레임(10초) 동안 새로 만든 오디오 노드가 0개**임을 확인했다
  (지시문이 명시한 "매 프레임 새로 만들지 마라"). 서버 값이 `undefined`·
  `NaN`·`null`·음수·`1e9` 로 와도 게인은 0~0.06 안에 머문다 — `NaN` 은
  falsy 라 `(speed || 0)` 에서 걸러진다. 헤드리스 크롬에서는 **진짜 Web Audio
  API** 로 같은 코드를 600프레임 돌려 예외 0건, 껐다 켜기·소리끔 경로까지
  확인했고(마스터 게인 0 으로), 게임 페이지를 띄워 **JS 콘솔 오류 0건**이다
  (남는 2건은 `index.html` 의 deprecated meta 와 PWA 배너 안내로, 고치기
  전에도 있던 것이고 소리와 무관하다). `node --check` 는 클라 JS 전 파일 통과.

  **못 한 검증:** 지시문은 `preview_start` 로 판을 시작하라고 하는데 이
  세션에는 그 도구가 없었다. 그래서 헤드리스 크롬으로 홈 화면까지만 띄웠고,
  **실제로 접속해 날면서 바람이 우는 것은 확인하지 못했다.** 다만 `wind()` 는
  서버가 늘 보내는 `sp`·`agl` 만 읽고(`game.py:1113·1123`), 그 두 칸은
  `main.js` 가 이미 다른 데서 읽고 있던 값이다.

- **부르는 곳이 없는 코드를 걷어냈다** (2026-08-14, 프로그래밍실) —
  옛 항공역학 분기가 사라질 때 계산식만 지우고 남은 잔재들이다.

  | 지운 것 | 어디 | 참조 |
  |---|---|---|
  | `sound_speed()` · `q_from_basis()` | `flightmath.py` | 0건 |
  | `quat.integrate` · `quat.fromBasis` | `m3d.js` | 0건 |
  | `groundH` · `airDensity` · `soundSpeed` · `RHO0` · `SCALE_H` | `m3d.js` | 0건 |
  | `CFG` 의 `gLimit` · `gLOC` · `stallWarnMargin` | `game.py` | 0건 |

  **남긴 것과 그 이유** — 이름이 비슷해 다음 사람이 헷갈릴 자리다:
  - `flightmath.q_integrate` 는 **살아 있다**(`game.py:341`, 잔해 회전).
    JS 짝인 `quat.integrate` 만 죽어 있었다. 자세를 각속도로 굴리는
    RESUME 의 막다른 길과는 무관하다 — 잔해는 조종하지 않는다.
  - `flightmath.air_density` · `RHO0` · `SCALE_H` 는 **살아 있다**
    (`game.py:716`, 미사일 항력). 죽은 것은 JS 쪽 짝뿐이다.
  - `m3d.G` 는 **살아 있다**(`world.js` 가 import 한다). 같은 줄에 있던
    `RHO0`/`SCALE_H` 만 떼어냈다.
  - **기체표(`AIRCRAFT`)의 `gLimit` 은 살아 있다** — `main.js:253` 이
    기체 선택 화면에 "30G" 로 찍는다. 지운 것은 이름이 같은 `CFG["gLimit"]`
    뿐이고 그쪽은 아무도 안 읽었다. **둘을 헷갈리지 마라.**

  검증: 봇 6기 60초 전투가 **고치기 전과 비트 단위로 같다** — 격추 12 /
  사망 12 / 생존 6 / 발사 165 / 기수각 179.9956도 / 비틀기 0.0000도,
  여섯 기의 위치·자세 해시까지 동일(`-6054872299032900906`). 지운 코드가
  실행 경로에 없었다는 가장 강한 증거다. 그 밖에 `py_compile` 3파일 통과,
  `node --check` 로 클라 JS 전 파일 통과, `m3d.js` 를 import 하는 8곳이
  전부 살아남은 이름만 부르는 것을 확인, 스냅샷 칸 수는 미사일 8칸 ·
  플레어 7칸 · 레이더 4칸이 클라와 일치(셋 다 빈 배열이 아닌 상태로 쟀다 —
  레이더는 시야를 50m 로 좁혀 강제로 채웠다).

- **무광 도장에 거울 반사가 걸려 있었다** (2026-08-14, 아트실) —
  `shadePBR` 이 환경 반사를 `normalize(reflect(-V, N))` 한 점에서 뽑았다.
  거친 면의 반사 로브는 넓게 퍼져 하늘을 넓게 평균해야 하는데 거울 표본은
  좁은 한 점을 집는다. 거칠기 0.62 무광 도장이 태양 둘레 미 후광을 그대로
  집어 표면이 통째로 떴다. 로브 중심 방향을 거칠기만큼 N 쪽으로 굽혔다
  (`mix(reflect(-V, N), N, rough*rough)`). 표본은 그대로 **한 번**이다 —
  `skyRadiance` 는 exp/pow/acos 덩어리라 두 번 부를 여유가 없다.

  **왜 중요했나:** 이 항은 알베도와 무관하다. 기체가 받는 빛의 35% 가
  알베도를 무시하니, 도장·패널라인·기류 때·그을음이 알베도를 아무리 흔들어도
  그 대비가 화면에서 절반 이하로 희석됐다. **"네 번 고쳤는데 똑같다"의
  메커니즘이 이것이다.** 고친 뒤 알베도 의존 비율 46.6% → 49.1%.

  실측(헤드리스, 실제 메시를 소프트웨어 래스터라이즈):
  | 항목 | 이전 | 이후 |
  |---|---|---|
  | 기류 때가 화면을 움직이는 폭 | 2.83 코드 | **3.48** |
  | 그을음 | 5.46 | **6.66** |
  | 패널별 광택차 | 1.83 | **2.16** |
  | 패널라인 | 0.90 | **0.95** |
  | 동체 옆면 밝기 | 186 코드 | 172 |
  | 주익 상면 | 157 | 153 |

  즉 **이미 있던 디테일이 전부 약 20% 더 보인다.** 새 디테일을 더한 게
  아니라 가리고 있던 막을 걷은 것이다.

  검증: WebGL2 컨텍스트에서 pbr 세 변형(기본 / SKIN / SKIN+DIRECT_OUT)에
  `mediump` 폴백과 `DIRECT_OUT` × `deriv` 조합까지 30개 프로그램 컴파일·링크
  통과 — 고치기 전과 **같은 결과**다(남는 2건은 이 파일이 이미 적어 둔
  WebGL2+ESSL1 의 `fwidth` 제약으로, 게임은 그 경로를 켜지 않는다).
  게임을 헤드리스로 띄워 **콘솔 오류 0건**, `progPbr` 이 폴백이 아닌 SKIN
  경로로 링크됨을 확인했다. 비용은 shadePBR 의 +0.89% 로 측정 반복 편차보다
  작다(ALU 6 op 증가 · `skyRadiance` 는 여전히 픽셀당 한 번).

  **지형·물도 같은 `shadePBR` 를 쓴다.** 부각 25도 이상에서는 1 코드 이내로
  안 바뀌고, 지평선 근처 스침각에서만 최대 8.5 코드 어두워진다(거기서는
  `aerial()` 안개가 이미 덮는다). 물리적으로도 그쪽이 옳다 — 거친 지면이
  하늘을 거울처럼 비추던 것이 사라진다.

  **막다른 길:** 이걸 metallic 이나 광택으로 되돌려 풀려고 하지 마라.
  방향이 정반대다 — 문제는 반사가 약해서가 아니라 **너무 또렷해서**였다.

- **계기판의 죽은 코드** (2026-08-14) — 부르는 곳이 없는 `pitchLadder()`,
  세팅하는 곳이 없는 `inp.stallGuard`, 서버가 상수로 보내는 `aoa` · `st`.
  셋 다 지웠다. 화면에는 영원히 `0.0°` 만 찍히는 칸과 절대 안 뜨는 경고
  두 개가 자리만 차지하고 있었다. 헤드리스 크롬으로 판을 띄워 계기판이
  찍는 글자를 가로채 확인했다 — `AOA` 만 사라지고 SPD·ALT·방위·G·THR·
  RKT·MSL·FLR·락온·MISSILE 경고는 그대로다. 콘솔 오류는 고치기 전과
  같은 1건(헤드리스에서 나는 pointer lock)뿐이다.
  피치 사다리는 **어지럽다고 걷어낸 것이다 — 되살리지 마라.**

- **발사 폭풍** — "사람 접속 시 초당 73발"로 적혀 있었으나 **재현되지 않는다.**
  2026-08-13 실측: 봇만 초당 3.0발, 사람 1명 초당 3.8발. 이론 최대 21발의
  15~18% 다. 이미 해결됐거나 애초에 잘못 측정한 값이었다.
- **화면 정보량 과다** — 꽉 찬 방에서 17.1KB 였다. 미사일이 시야 안에 450발까지
  잡혀 통째로 실려 나갔다. 가까운 것부터 상한만큼만 보내고 좌표를 1m 단위로
  줄여 8.5KB 가 됐다. 접속자당 초당 428KB → 208KB.
- **검은 네모 깜빡임** — 캔버스의 투명 픽셀은 RGB 가 0 인데 밉맵이 그것을
  섞어 스프라이트를 어둡게 만들었다. 아틀라스를 날 배열로 올려 해결.
- **기총 잔재** — 기총을 없앤 뒤에도 총알 시뮬레이션이 매 틱 돌고 있었다.
  서버·클라이언트 양쪽에서 전량 삭제.
