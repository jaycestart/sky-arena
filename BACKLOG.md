# 일감 — AI 회사가 여기서 골라 간다

자율 에이전트가 사람 지시 없이 깨어났을 때 **여기 맨 위부터** 하나 집는다.
하나만 한다. 여러 개를 한 번에 건드리면 무엇이 화면을 망가뜨렸는지 되짚을 수 없다.

## 규칙

1. **main 에 직접 푸시하지 않는다.** 반드시 브랜치를 파고 PR 을 연다.
   main 은 Render 로 자동 배포된다 — 사람이 안 본 코드가 배포되면 안 된다.
2. **고쳤다고 주장하려면 숫자를 대라.** 이 저장소의 검증은 전부 헤드리스로
   돌릴 수 있다(`.github/workflows/ci.yml` 참고). 화면을 눈으로 못 보는
   환경이므로 "좋아 보인다" 는 근거가 아니다.
3. **RESUME.md 의 '막다른 길' 을 먼저 읽어라.** 이미 네 번 실패한 접근이
   적혀 있다. 다시 시도하면 시간만 버린다.
4. 조작·무기·물리를 바꾸면 **봇에도 같이 적용**한다(`_bot_point`, `_bot_think`).
5. 일감을 끝내면 이 파일에서 지우고, 새로 발견한 것은 아래에 추가한다.

---

## 1순위 — 발사 폭풍 (원인 미상)

봇만 있는 방에서는 초당 2.5발인데, **사람이 접속하면 초당 73발**로 뛴다.
이론 상한은 6기 기준 21발/초다.

이미 배제한 것:
- 틱 속도 정상 (실측 49.2/s, 정상 50)
- `fire_t` / `ms_cd` 감소 코드 존재 (`Plane.step`)
- 명중한 미사일 제거 정상 (`_step_missiles` 의 `continue`)

남은 의심:
- 사람 접속 경로에서만 도는 무언가 (`Room.session` / `handle`)
- 봇 재출격이 `spawn()` 에서 `fire_t` 를 0 으로 되돌리는 것
- `_fill_with_bots` 가 기체를 과하게 채우는지

재현: 서버를 띄우고 WebSocket 으로 join 한 뒤 5초간 `launch` 이벤트를 센다.
CI 의 "발사 속도가 쿨다운을 지키는가" 가 회귀 감시만 하고 있으니, 원인을
잡으면 그 검사의 임계를 조여라.

## 2순위 — 기체가 플라스틱처럼 보인다

메시에는 노즐 페탈·미도장 내열합금·흡입구 덕트·LERX·캐노피가 다 있는데도
플라스틱으로 읽힌다. **부품을 더 붙여서는 해결되지 않는다.** 표면이 그렇게
읽히는 원인을 찾아라 — 재질 파라미터, 단색 알베도, 환경 반사 부재,
면 단위 법선 중 어느 것이 지배적인지 근거를 대고 거기에 집중할 것.

## 3순위 — 저공 속도감

순항 고도 3000m 에서는 속도가 안 느껴진다. 지형 최고 주파수 파장이 885m 라
**885m 보다 작은 지물이 원리적으로 없다** — 나무를 심어도 1픽셀이 안 된다.
카메라 근처 공기 입자는 한 번 넣었다가 사용자가 싫다고 해서 걷어냈다
(`speedStreaks`, 커밋 이력 참고). 저공(AGL 600m 이하)에서만 도는 지상
스캐터가 남은 후보다.

## 4순위 — 미확인 사항 (사람 눈이 필요)

이 환경은 브라우저 패널이 숨겨져 있어 rAF 가 멈춘다. 아래는 수치로만
확인했고 **실제 화면은 아무도 안 봤다.** 사람에게 물어볼 것:
- 조준 리시 55도가 답답한지
- 3인칭에서 기체가 화면에서 미끄러지는 느낌이 없는지
- 마우스 감도 cm/360 기본값 12 가 적당한지
