# 파이썬
__pycache__/
*.py[cod]

# 서버가 돌면서 남기는 것 — 기기마다 다르고 되돌릴 가치가 없다
server.log
server.err.log
*.log

# 편집기 · OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# 임시 산출물
*.tmp
eqv-tmp.json

# 작업 중 만들어지는 임시 파일. 실수로 커밋되면 배포에 섞인다.
public/js/gfx/__*
# 부서가 검증용으로 만드는 임시 파일. public/ 어디에 만들든 걸리게 한다 —
# gfx/ 아래만 막아뒀더니 아트실이 public/ 바로 밑에 만들어 새어 나왔다.
public/__*
