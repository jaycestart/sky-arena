# 만들 것 — 진행 구조 (2026-08-15)

이 문서는 **사장님이 정한 방향**이다. 부서가 바꾸지 마라. 여기 적힌 것을
한 단계씩 실제로 만드는 것이 앞으로의 근무다.

## 왜 이 문서가 생겼나

8월 14일 하루에 커밋이 열여섯 건 올라갔다. 전부 옳은 일이었다 — 죽은
코드를 지웠고, 주석의 틀린 숫자를 고쳤고, 안 읽는 스냅샷 칸을 걷어냈다.
그리고 사장님은 게임을 켜고 이렇게 말했다:

> "게임도 하나도 안 변해 있어."

맞는 말이다. **정리는 사장님 화면에 아무것도 남기지 않는다.** 지금 이
게임에는 한 판 하고 나면 남는 것이 없다. 격추해도 숫자만 오르고, 판이
끝나면 그 숫자도 사라진다. 다시 켤 이유가 없다.

## 방향 — 정통 구조로 간다

사장님이 못박은 것: **참신한 자작 메커니즘 말고, 골드 · 레벨 · 아이템 ·
스킬.** 다들 아는 그 구조 그대로다. 새로운 것을 발명하지 마라.

    싸운다 → 골드와 경험치를 얻는다 → 기체를 강화한다 → 더 잘 싸운다

## 단계 — 위에서부터 하나씩. 아랫단계를 먼저 하지 마라

### 1단계 · 판이 끝나면 결과가 뜬다  (UI실)

지금은 죽거나 나가면 그냥 화면이 넘어간다. 판이 끝날 때 화면을 띄운다:

    격추 7 · 사망 3 · 최고 연속 3
    ─────────────────────────
    격추 7 × 100        700 G
    연속 보너스          150 G
    생존 4분 20초        260 G
    ─────────────────────────
    이번 판              1110 G
    가진 골드            3420 G

골드는 `localStorage['skyarena.gold']` 에 누적한다. 서버 계정이 없으니
브라우저에 쌓는 것이 맞다 — 외부 서비스를 붙이지 마라.

보상표(이 숫자를 그대로 써라):

| 무엇             | 골드 | 경험치 |
|------------------|------|--------|
| 격추 1기         | 100  | 100    |
| 어시스트         | 30   | 30     |
| 생존 1분         | 60   | 20     |
| 연속 격추 (n≥2)  | 50×n | 50×n   |

### 2단계 · 레벨이 오른다  (UI실)

경험치도 같이 쌓는다. `localStorage['skyarena.exp']`.
필요 경험치는 **레벨 × 1000** 이다 (1→2 는 1000, 2→3 은 2000 …).
레벨이 오르면 결과 화면에 크게 띄우고, **스킬 포인트 1**을 준다.

HUD 한쪽 구석에 늘 `Lv.7  3420 G` 를 작게 띄운다.

### 3단계 · 격납고에서 골드를 쓴다  (UI실 + 프로그래밍실)

시작 화면에 **격납고** 단추를 만든다. 강화는 넷뿐이다. 각 5단계.

| 강화     | 단계당 효과      | 1단계 값 | 값 증가 |
|----------|------------------|----------|---------|
| 엔진     | 추력 +4%         | 400 G    | ×1.8    |
| 장갑     | 최대 체력 +6%    | 400 G    | ×1.8    |
| 조준기   | 로켓 탄속 +3%    | 500 G    | ×1.8    |
| 유도장치 | 유도탄 선회 +5%  | 500 G    | ×1.8    |

산 것은 `localStorage['skyarena.upg']` 에 `{"engine":3,...}` 로 둔다.
접속할 때 net.js 의 첫 메시지에 `upg` 를 실어 보내고, **서버가 그 값을
기체 계수에 곱한다.** 클라이언트가 물리를 고치면 예측이 어긋난다.

서버는 받은 값을 **반드시 0~5 로 자른다.** 브라우저가 보내는 값은
사장님 것이든 남의 것이든 믿을 수 없다.

### 4단계 · 스킬 포인트를 쓴다  (그때 가서 다시 적는다)

3단계까지 실제로 굴러가는 것을 보고 나서 정한다. 미리 만들지 마라.

## 지킬 것

- **한 근무에 한 단계.** 반쯤 만든 단계를 커밋하지 마라.
- 봇도 사람과 같은 규칙을 받는다. 봇에게 강화를 주지는 않되, 강화가
  붙은 사람과 붙어도 판이 일방적이 되지 않는지 60초 전투로 확인한다.
- 골드·경험치는 **판이 끝날 때 한 번** 쓴다. 매 틱 localStorage 를
  건드리면 프레임이 튄다.
- 아직 없는 값을 읽지 마라. 어시스트와 연속 격추를 서버가 안 보내면,
  1단계는 격추·사망·생존시간만으로 하고 나머지는 BACKLOG 에 넘겨라.
