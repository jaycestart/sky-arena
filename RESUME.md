# SKY ARENA — 작업 중단 지점 (2026-08-08)

## 실행

```bash
python C:/Users/jayce/sky-arena/server/app.py
```

브라우저에서 `http://127.0.0.1:8080/` — 같은 와이파이의 다른 기기는 홈 화면의
"주소 복사" 버튼에 뜨는 LAN 주소로 접속한다. 의존성 없음(파이썬 표준 라이브러리만).
node 가 깔려 있지 않으므로 JS 는 로컬 린트가 불가능하다 — 고친 뒤에는 브라우저에서
모듈 import 부팅 검사를 해야 한다.

## 지금 상태

마지막 작업은 **조작 체계 재설계**였다. 요구사항은 두 줄로 요약된다.

- 마우스로는 **절대 기체가 비틀리지 않는다**(기수를 축으로 도는 회전 = 롤).
- 마우스로 **상하좌우 360도 제한 없이** 움직인다. A·D 만 기체를 비튼다.

### 조준의 주인은 input.js 다 (2026-08-08 재설계)

조준 방향 `input.aim` 을 **월드 단위벡터** 상태로 두고, 카메라·기수 명령·HUD·
서버 전송이 전부 같은 프레임의 그 값 하나를 읽는다. 예전 폐루프
(마우스 → scene.viewYaw/Pitch → camQ → 한 프레임 늦게 aimDir → 기수 →
다시 카메라의 기체 추종)를 한 방향으로 끊은 것이다.

- `input.updateAim()` — 마우스 원본 카운트를 **화면축(카메라 right/up) 둘레로
  벡터 회전**시킨다. hdg/pit 스칼라 적분이 아니므로 극점 축퇴가 없다.
  감도는 cm/360 한 곳에서만 곱한다.
- **리시(leash)** — 조준점이 기수에서 기본 55도 이상 벌어지지 않게 초과분만
  당긴다. 기체 회전량은 전혀 제한하지 않는다(계속 밀면 무한히 돈다).
- `input.sample()` — 오차를 **차트축**(ê_h = 방위 증가, e_p = 기수각 증가)으로
  분해하는 P 제어기(KP=9). 전각도 θ 로 스케일해 180도 사각지대가 없다.
  수직 근처(|cos pit| < 0.35)에서는 요 권한을 페이드아웃하고 피치로 넘긴다 —
  넘길 방향은 |dP|>0.15 면 sign(dP), 아니면 **정점 쪽**(sign(sin pit)).
  sign(dP) 만 쓰면 dP≈0 에서 부호가 매 틱 뒤집혀 제자리 떨림이 된다(실측).
- `scene.camera()` — 상태 없는 함수. `q = swing(기수→aim) ⊗ q_plane`.
  최단호 스윙은 기수축 둘레 비틀림이 정의상 0 이라, 화면 기울기가 bnk
  하나에서만 온다는 것이 **기하학적 항등식**이 된다.
- 서버는 `AIRCRAFT` 회전율과 `aim_dir` 원뿔(2.60→1.15rad)만 바뀌었다.
  `Plane.step` / `world._integrate` 적분식은 한 글자도 안 건드렸다.

### 자세 모델 (이번 작업의 핵심)

자세를 각속도로 굴려 쌓지 않는다. **방위(hdg) · 기수각(pit) · 비틀기(bnk)**
세 값이 원본이고, 매 틱 `q_hpb(hdg, pit, bnk)` 로 자세를 새로 만든다.

- 서버: `server/flightmath.py` 의 `q_hpb` / `wrap_pi`, `server/game.py` 의 아케이드 분기
- 클라이언트 예측: `public/js/world.js` `_integrate` (서버와 같은 식)
- 카메라: `public/js/scene.js` `camera()` — 시선도 같은 `quat.hpb` 로 만든다

세 각도 모두 제한이 없다. 계속 당기면 수직을 넘어 한 바퀴 돈다.

**스냅샷에 `hpb` 를 함께 내려보낸다.** 기수각이 90도를 넘으면 자세(쿼터니언)에서
세 값을 되뽑을 때 뒤집힌 해가 나와 조작이 반대로 먹는다. 그래서 클라이언트도
자세에서 되뽑지 않고 세 값을 상태로 들고 간다 (`world.me.hpb`).

### 막다른 길 네 개 — 다시 시도하지 말 것

1. **롤 명령만 0 으로 두기** — 피치와 요를 동시에 주면 기구학적으로 롤이 딸려
   붙는다. 6초 조작에 35도까지 기울었다.
2. **딸려 온 롤만 지우기**(평행이동, `roll_free`) — 구면 위 평행이동은 닫힌
   경로를 돌면 누적 회전이 남는다. 역시 35도.
3. **세계 기준으로 기울기를 되맞추기**(`set_bank`) — 기울기는 0 이 되지만
   기수가 수직을 넘는 순간 수평 기준이 180도 뒤집혀 화면이 홱 돈다.
4. **조준을 aimYaw/aimPitch 스칼라 두 개로 적분하기** — 수직에서 1/cos(pit) 이
   폭주하고 마우스 좌우가 화면 굴림이 된다. 극점에서 요를 1/|cos p| 로
   **증폭**하는 것도 같은 이유로 안 된다(사용자 1순위 요구를 정면으로 깬다).
   조준은 반드시 월드 벡터를 화면축 둘레로 돌려야 한다.

### 검증 수치 (마지막 측정)

| 항목 | 결과 |
|---|---|
| 마우스만 급조작(피치+요) | 비틀기 0.0000도 |
| 수평 선회 3초 | 비틀기 0.0000도, 고도 변화 0m |
| 계속 당기기 8초 | 기수 802도(2.2바퀴), 한 틱 최대 2.01도 |
| 서버 왕복 250스냅샷 | 비틀기 0.0000도, 기수 999도 통과 |
| 클라이언트 예측 vs 서버 (200틱) | 차이 0.000000도 |
| A 4초 유지 / 뗀 뒤 | -1192도 연속 회전 / 유지 오차 0.0000도 |
| 카메라 900프레임(수직 여러 번 통과) | 프레임당 최대 1.08도, 튐 없음 |

### 조작 재설계 검증 수치 (2026-08-08, 헤드리스 실측)

| 항목 | 결과 |
|---|---|
| 조준 1:1 (100카운트, cm360=12) | 9.525041도 — 이론값과 일치 |
| 같은 검사, 비틀기 90도 / 기수각 89도 | 9.525041도 (자세 무관, 극점 무영향) |
| 대각선 입력(+50,−50) 화면각 | 44.90도 (비가환 오차 0.10도) |
| 기수 추종 정상상태 오차(20도 오프셋, 1초) | 0.0014도 (예전엔 데드존 1.4도에서 정지) |
| 직선 접근(대원호 방향과의 각차) | 최대 1.17도 |
| 비틀기 0/45/90/180 에서 위 두 값 | 동일, 비틀기 표류 0.000000도 |
| 기수각 88도 + 옆 조준점 120틱 | 정점 넘어감(88→115도), 비틀기 0.000000도, 1.1초에 수렴 |
| 마우스만 급조작 300틱 | 비틀기 0.000000도, 방위 71도 변화 |
| 리시: 같은 방향 600틱 | 조준 벌어짐 55.000도 고정, 방위 누적 1658도(무제한) |
| 카메라: aim 고정 + A 4초 | 카메라 정면 변화 0.000001도 (화면만 −15도 굴러감) |
| 카메라: 기수각 0→2π 900프레임 | 프레임당 최대 0.4도, 수직 통과 튐 없음 |
| 외부 시점 프레이밍(0~55도 × 5방향) | 전부 화면 안 |
| 60도 옆 조준점 수렴 | 10도까지 0.35초 / 1도까지 0.58초 (운동학 하한 0.36초) |
| 계속 당기기 8초 | 한 틱 최대 2.4828도 = pitchRate·dt 정확히 일치, 튐 없음 |
| 클라 예측 vs 서버 200틱 | hpb·pos·vel **비트 단위 동일** |
| aim_dir 원뿔 | 40도 통과 / 66·70·120도 → 65.89도로 절단 |

### 무기 (2026-08-09)

기총을 없앴다. 좌클릭 = 무유도 로켓(빠르고 아프다), 우클릭 = IR 유도탄(덜 아프다).
`Missile` 이 `kind` 를 들고 다니고 `_step_missiles` 가 `WEAPONS[m.kind]` 를 쓴다.
`spec["seeker"] <= 0` 이면 표적 탐색·유도를 통째로 건너뛴다. `Bullet` 클래스와
`_fire_gun` 은 지웠고 스냅샷의 `b` 키는 빈 배열로 유지한다(프로토콜 불변).

### 파티클 검은 네모 (2026-08-09)

Canvas2D 의 투명 픽셀은 RGB 가 0 이다. 알파를 미리 곱하지 않은 채 밉맵을
만들면 '투명한 검정'이 평균에 섞여 스프라이트 사각형이 검게 나온다.
**캔버스에 되써 넣는 걸로는 못 고친다** — 캔버스가 내부적으로 알파를 곱해
저장해서 알파 0 픽셀의 RGB 는 다시 0 이 된다. `buildAtlas` 가 캔버스 대신
`{data, size}` 원시 배열을 돌려주고 `uploadAtlas` 가 그걸 직접 올린다.

### 지면 속도감 (2026-08-09)

디테일 옥타브 페이드가 '2픽셀 미만이 되는 거리'보다 2.3~2.6배 일찍 꺼져
있었다. 출격 고도 3200m 에서는 **지면 시야의 95% 에 아무 디테일도 없었다**.
페이드를 한계의 70% 로 늘리고(30m→7.4km, 8m→1.97km, 2m→490m), 알베도
얼룩을 얹었다. 노멀 섭동은 스치는 빛에서만 보이므로 밝기 자체가 변해야
광류가 생긴다. `macro` 를 재활용해 `fbm2` 호출은 한 번만 늘렸다.

### 봇에도 같이 적용한다 (2026-08-09, 사용자 지시)

조작·무기·물리를 바꿀 때 **사람 쪽만 고치고 끝내지 않는다.** 봇 조종기는
`_bot_point` 에 있고 `input.js sample()` 을 옮겨 적은 것이라, 한쪽만 고치면
봇과 사람이 다른 게임을 하게 된다. `BOT_CHART_AXIS` 는 이제 `True` 다.

이때 함께 넣은 것:
- `_bot_keep_in` — 새 제어기는 표적만 보고 직선으로 아레나를 이탈한다(반경
  4500m 밖 초당 20 피해). 0.62R 부터 안쪽으로, 6500m 위·1200m 아래에서
  수직으로 당긴다. 옛 뱅크투턴은 원반 선회라 저절로 안을 돌아 필요 없었다.
- 봇 로켓 리드를 등가속 해로 바꿨다. 로켓은 발사 후 1000 m/s²
  (`thrust` 60000 / `mass` 60) 로 계속 가속해서 `muzzle` 로 잡으면 비행시간을
  길게 보고 표적을 크게 앞질러 겨눈다.
  (여기 적혀 있던 15000 은 근거 없는 숫자였다 — 2026-08-14 정정.)
- 봇 유도탄 사거리 조건 1200m → 600m. 실제 교전 거리 중앙값이 151m 라
  1200m 조건에서는 **한 발도 안 쐈다**.

**무기별 무장 지연** (`WEAPONS[*]["arm"]`) 도 이때 나왔다. 전 무기 0.35초
고정이었는데 로켓은 초속 2000m 라 700m 를 날아간 뒤에야 신관이 살아났다 —
그 안쪽 적은 그냥 관통했다. 사람도 똑같이 당하던 버그다. 로켓 0.06초(117m),
유도탄 0.35초(461m).

검증(봇만 6기, 60초 헤드리스): 격추 17회 = 사망 17회(이탈사 0), 유도탄 25발,
기수각 최대 180도(수직 기동), 비틀기 0.0도, 고도 1975~6680m.
고치기 전에는 격추 2회 / 사망 16회(그중 14회가 이탈사)였다.

## 남은 일

- **조작 재설계는 사용자가 눈으로 확인하지 않았다.** 이 박스의 브라우저 패널이
  숨겨져 있어 rAF 가 멈춘다(fps 0, 캔버스 1×1). 수치는 전부 `window.sky` 로
  손수 프레임을 돌려 잰 것이고, 실제 렌더 화면 검증은 사용자 쪽에서만 된다.
  특히 (a) 55도 리시가 답답한지, (b) 외부 시점에서 기체가 화면에서 미끄러지는
  느낌이 없는지, (c) cm/360 기본값 12 가 적당한지.
- **봇 조종기는 아직 옛 뱅크투턴이다.** `server/game.py` 의 `BOT_CHART_AXIS`
  를 `True` 로 바꾸면 사람과 같은 차트축 제어기로 갈아끼워지고 `gPull` 이
  자동으로 0.75배가 된다. 조작 변경과 원인을 분리하려고 **일부러 꺼 두었다** —
  위 (a)~(c) 를 확인받은 뒤에 켤 것.
- FOV 속도 부스트 구간의 감도 보정 방향은 사양서와 반대로 넣었다
  (`tan(fov/2)/tan(fovBase/2)`). 사양서 식은 화면 감도를 두 배로 느리게 만든다.
  근거는 `input.js` `updateAim` 주석에 있다.
